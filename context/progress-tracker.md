# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Feature 29 (spec-ui-integration) — complete

## Current Goal

- To be determined for the next feature unit. Feature 29 wired the Specs tab up to real data (list, Markdown preview, download) — see Completed below for the full design. Still open: the "Generate Spec" button in that same tab is still inert (triggering `generate-spec` from the UI was explicitly out of this unit's scope), and `trigger/generate-design.ts` is still an unused stub — the "three overlapping task files" open question from 22 still only concerns `generate-design.ts` vs. `design-agent.ts`.

## Completed

- 01-design-system: shadcn/ui installed and configured (Tailwind v4, `base-nova` style, `neutral` base color, Base UI primitives). Added Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea to `components/ui/`. Installed `lucide-react`. `lib/utils.ts` has `cn()`. Verified: all 7 components import cleanly, `cn()` merges classes correctly, `tsc --noEmit` and `next build` pass.
- 02-editor: added the Ghost-specific design tokens from `ui-context.md` to `app/globals.css` (`--bg-base`, `--bg-surface`, `--bg-elevated`, `--bg-subtle`, `--border-default`, `--border-subtle`, `--text-primary/secondary/muted/faint`, `--accent-primary(-dim)`, `--accent-ai(-text)`, `--state-error/success/warning`) and mapped them in `@theme inline` to Tailwind utilities: `bg-base/surface/elevated/subtle`, `border-surface-border(-subtle)`, `text-copy-primary/secondary/muted/faint`, `text-brand`/`bg-brand`, `bg-accent-dim`, `text-ai`/`bg-ai`, `text-ai-text`, `*-error/success/warning`. These tokens existed only as documentation in `ui-context.md` before this — they were never wired into `globals.css` during 01-design-system, so nothing in the app could satisfy the "no raw Tailwind colors/hex, use these token names" rule in `code-standards.md` until now.
- 02-editor: `components/editor/editor-navbar.tsx` — fixed-height (`h-14`) client component with left/center/right sections, `bg-surface` background, `border-surface-border-subtle` bottom border. Left section holds a sidebar-toggle icon button (`PanelLeftOpen`/`PanelLeftClose` from lucide-react, swapped based on `isSidebarOpen` prop). Center and right sections are empty placeholders for future chapters. Controlled via `isSidebarOpen`/`onToggleSidebar` props (no internal state — the parent editor page will own this once it exists).
- 02-editor: `components/editor/project-sidebar.tsx` — floating overlay panel (`fixed`, `z-40`), positioned clear of the navbar with a small margin on all sides, `bg-elevated/95` + `backdrop-blur-sm` + `border-surface-border`, `rounded-2xl`. Slides in/out via `translate-x` transition driven by the `isOpen` prop; `pointer-events-none` + `aria-hidden` when closed. Header has "Projects" title + close button (`onClose` prop). Body uses shadcn `Tabs` ("My Projects" / "Shared"), each with an independent empty placeholder (FolderOpen icon + message). Footer has a full-width `New Project` button with a `Plus` icon.
- 02-editor: verified the existing `components/ui/dialog.tsx` (shadcn Dialog, already supports Title/Description/Footer, styled with the pre-existing shadcn tokens in `globals.css`) already satisfies the "dialog pattern ready for future use" requirement — no changes made, since actual dialogs are explicitly out of scope for this unit and `components/ui/*` is a protected foundation component.
- 02-editor: verified via `tsc --noEmit` (clean) and `eslint` (clean on new files) and by driving a temporary preview route with Playwright/Chromium against `next dev` (toggle behavior, slide animation, tab switching, empty states all confirmed visually, no console errors); the temporary route was deleted after verification.
- 02-editor: `EditorNavbar` + `ProjectSidebar` wired into a real route at `app/editor/page.tsx` (client component owning `isSidebarOpen` state). Pulled forward from "Next Up" because 03-auth requires a real `/editor` destination (post-sign-in redirect target, and host for the navbar's `UserButton`). The page body is a placeholder ("Canvas coming soon") — no canvas implementation yet, that's a separate future chapter.
- 03-auth: `proxy.ts` added at the project root (Next.js 16 renamed `middleware.ts` → `proxy.ts`; the old convention is deprecated but still works as a fallback). Uses `clerkMiddleware` + `createRouteMatcher` from `@clerk/nextjs/server`. Public routes are derived from `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL` (`(.*)` suffixed so nested Clerk sub-routes like SSO callback are covered); every other route calls `auth.protect()`. Matcher excludes `_next` and static asset extensions, and explicitly includes `/api` and `/trpc`.
- 03-auth: added `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in` and `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up` to `.env.local` — these are Clerk's own standard env var names (read directly by `@clerk/nextjs`'s internals), not invented ones. Only `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`/`CLERK_SECRET_KEY` existed before this.
- 03-auth: installed `@clerk/ui` (`^1.29.1`). `app/layout.tsx` now wraps `<html>` in `ClerkProvider` with `ui={ui}` (bundles Clerk's UI locally instead of loading from CDN) and `appearance={{ theme: dark, variables: {...} }}` using `dark` from `@clerk/ui/themes` as the base theme. Every `variables` entry is a `var(--...)` reference into the app's existing Ghost tokens (`--accent-primary`, `--bg-elevated`, `--state-error`, etc.) — no hardcoded colors. `borderRadius` uses `calc(var(--radius) * 1.8)` to match the app's `rounded-2xl` card convention.
- 03-auth: `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` (Next.js optional catch-all segments, required for Clerk's internal multi-step routing e.g. SSO callback) render Clerk's `<SignIn />`/`<SignUp />` inside a new shared `components/auth/auth-layout.tsx`. Below `lg`, only the form column renders.
- 03-auth: `components/auth/auth-layout.tsx` redesigned to match a supplied reference screenshot: true 50/50 split at `lg` (`w-1/2` both sides), left panel uses `bg-surface` against the right panel/page's `bg-base` so the two halves read as distinct flat dark shades (no gradient, per the original spec's "no gradients" rule) — this directly satisfies "some kind of color on the left side to differentiate it from a dark background". Left panel: small `bg-brand` logomark square + "Ghost AI" wordmark at top, a large bold heading + subtitle, a 3-item feature list (each item: small `bg-accent-dim`/`text-brand` icon badge — lucide `Sparkles`/`Share2`/`FileText`, all stroke icons — plus title and description; no per-item card/border/background, so still consistent with "no feature cards"), and a copyright line pinned to the bottom.
- 03-auth: fixed a pre-existing bug in `app/globals.css`'s `@theme inline` block — `--font-sans` was self-referencing (`--font-sans: var(--font-sans)`), which is an invalid circular reference, so the `font-sans` Tailwind utility (applied to `html` in the base layer) silently fell back to the browser default font instead of Geist Sans, even though `next/font` was correctly loading Geist and exposing it as `--font-geist-sans` on `<html>`. Changed `--font-sans`/`--font-heading` to point at `var(--font-geist-sans)` and added `--font-mono: var(--font-geist-mono)` (was missing entirely) so the `font-mono` utility works too. This bug predates 03-auth and affected the whole app, not just auth pages — verified via computed `font-family` in the browser (now resolves to `Geist, "Geist Fallback"` instead of a generic fallback stack).
- 03-auth: `app/page.tsx` converted to an async Server Component — calls `auth()` from `@clerk/nextjs/server` and redirects to `/editor` if `userId` is present, otherwise `/sign-in`. This is on top of (not instead of) proxy-level protection: `/` isn't in the public-route list, so an unauthenticated request is already redirected by `auth.protect()` in `proxy.ts` before this page's own redirect would even run; the page-level check exists to satisfy the authenticated→`/editor` case, which proxy-level protection alone doesn't provide.
- 03-auth: added Clerk's `UserButton` to `EditorNavbar`'s right section (`components/editor/editor-navbar.tsx`) for profile settings/logout, per spec. No other navbar changes.
- 03-auth: verified via `tsc --noEmit` (clean), `npm run build` (passes, `proxy` shows as registered middleware in the build output), and a Playwright/Chromium pass against `next dev`: confirmed unauthenticated `curl` to `/` and `/editor` both 307-redirect to `/sign-in` (with `redirect_url` preserved for `/editor`); visually confirmed the two-panel dark layout on desktop, form-only on mobile (no horizontal scroll), dark theme applied with no light-theme bleed-through, and no console errors on either auth page at either viewport. The only console message was Clerk's expected "development keys" dev-mode notice.
- 04-project-dialogs: `types/project.ts` (`Project { id, name, slug, isOwner }`), `lib/slug.ts` (`slugify()`), and `lib/mock-projects.ts` (3 seeded mock projects, 2 owned + 1 shared) added as the mock data layer for this unit — no API routes or Prisma involved, per spec's "mock project data only" constraint.
- 04-project-dialogs: `hooks/use-project-dialogs.ts` — the single dedicated hook required by the spec's Implementation section. Owns the mock project list plus a discriminated-union `dialog` state (`null | {type:"create"} | {type:"rename",project} | {type:"delete",project}`), the shared `name` form field (with a derived `slugPreview` via `useMemo`+`slugify`), and an `isLoading` flag set around a `mockDelay()` (`setTimeout`, 400ms) inside each `submitCreate`/`submitRename`/`submitDelete` — the delay exists only to make the required "loading state" observable, not to simulate a real request.
- 04-project-dialogs: `components/editor/editor-home.tsx` — center content for `/editor` (heading, description, `New Project` button with `Plus` icon), no card wrapper, reproduces the spec's copy verbatim including its "choose, a project" comma.
- 04-project-dialogs: `components/editor/project-dialogs.tsx` — three `Dialog`s (Create/Rename/Delete) driven entirely by `useProjectDialogs()`'s return value (passed in as `state` prop). Create has a name input with live slug preview text below it. Rename is prefilled from the target project, autofocuses its input (relies on Base UI's `DialogPortal` unmounting `Popup` when closed, so `autoFocus` re-fires on every open), and submits on Enter via a wrapping `<form onSubmit>`. Delete has no input, just a description naming the project and a `variant="destructive"` confirm button.
- 04-project-dialogs: `components/editor/project-sidebar.tsx` extended (not replaced) to render actual project rows per tab instead of only the empty state — owned projects get inline `Pencil`/`Trash2` icon buttons (`onRenameProject`/`onDeleteProject` callbacks), shared/collaborator projects (`isOwner: false`) render name-only with no action buttons, satisfying "show actions only for owned projects." Added a `lg:hidden` backdrop scrim (`fixed inset-0 bg-black/50`, `z-30`, below the sidebar's `z-40`) that closes the sidebar on click — desktop is unaffected since the scrim never renders at `lg` and up.
- 04-project-dialogs: `app/editor/page.tsx` now instantiates `useProjectDialogs()` once and wires it to all four entry points named in the spec: editor-home `New Project` → create dialog, sidebar footer `New Project` → create dialog, sidebar item rename/delete icons → rename/delete dialogs. `EditorNavbar` and the sidebar's own open/close behavior were left untouched, per the Editor Home section's "do not modify the navbar or sidebar behavior."
- 04-project-dialogs: verified via `tsc --noEmit` (clean), `eslint .` (clean), `npm run build` (passes), and a Playwright/Chromium pass driving a temporary public route (`app/editor-preview-tmp`, plus a temporary `proxy.ts` public-route entry, both reverted/deleted after use — same pattern as the 02-editor session) — confirmed: slug preview updates live and strips punctuation/casing correctly; rename dialog prefills, autofocuses, and Enter-submits with the input's value fully replacing the old name (not appended — the first pass falsely suggested otherwise because the test script's `Ctrl+A` didn't select-all on this OS, not an app bug; re-verified with `.fill()`); delete dialog has zero inputs and a destructive-styled confirm button; rename/delete icons are present for owned projects and absent for the shared project; mobile viewport (390×844) shows the backdrop scrim and a tap on it closes the sidebar; no console errors.
- 04-project-dialogs bugfix: the Rename dialog's input text was rendering unreadably dark (near-black on a dark surface) — user caught this from a screenshot. Root cause was in `app/globals.css`'s `@theme inline` block, predating this unit: `--color-base: var(--bg-base);` registered a Tailwind v4 color token named "base". Tailwind v4 shares one namespace across all color-utility prefixes (`bg-`, `text-`, `border-`, etc.), and "base" collides with Tailwind's own built-in font-size scale key (`--text-base: 1rem`, used by the `text-base` utility). The compiled CSS showed `.text-base { color: var(--bg-base); }` with no font-size rule at all — so every `text-base` usage in the app (in the protected `components/ui/input.tsx`, `textarea.tsx`, `card.tsx`, `dialog.tsx`, plus `components/auth/auth-layout.tsx`) had silently lost its font-size and gained an unwanted near-black text color. It was only visibly broken where nothing else overrode the color (the dialog `Input`/`DialogTitle`); in `auth-layout.tsx` an adjacent `text-copy-primary`/`text-copy-muted` class happened to win the cascade, masking the bug there.
- Fixed at the root: renamed the token from `--color-base` to `--color-page` in `app/globals.css` (no other token collides with a built-in Tailwind scale key — verified by checking `node_modules/tailwindcss/theme.css`), which un-shadows the real `text-base` font-size utility without touching any protected `components/ui/*` file. Updated the two real `bg-base` usages (both in `components/auth/auth-layout.tsx`) to `bg-page`. `app/layout.tsx`'s Clerk theme config reads `var(--bg-base)` directly (a raw CSS var, not a Tailwind class) and was unaffected. `context/ui-context.md` updated to document `bg-page` (not `bg-base`) as the page-background utility name, with a pointer back to this note.
- 04-project-dialogs code-review fix: `components/editor/project-dialogs.tsx`'s three `onOpenChange` handlers now skip `closeDialog()` while `isLoading` is true, so Escape/backdrop-click/the X button can no longer dismiss a dialog mid-submit. Verified via Playwright: all three dismissal paths are blocked while a mock submit is pending, the dialog still closes itself the instant the submit resolves, and normal (not-loading) dismissal is unaffected. A second reported finding — binding `submitCreate`/`submitRename`/`submitDelete` to their originating dialog so a stale completion can't mutate `projects` or call `closeDialog()` after the dialog was dismissed — was checked and found not reachable in the current code: dismissal is the only way `dialog` state could change mid-submit (every trigger button lives behind the modal dialog's backdrop, `z-40` sidebar vs. `z-50` overlay), and this fix removes that path, so no separate operation-binding was added.
- 04-project-dialogs code-review fix: `slugify()` can return `""` for a non-empty, non-whitespace name (e.g. `"!!!"` — passes the existing `name.trim()` check but strips to nothing), which let `submitCreate`/`submitRename` in `hooks/use-project-dialogs.ts` write a `Project` with an empty `slug`. Both now also compute `slug = slugify(trimmed)` and return early when it's empty. `components/editor/project-dialogs.tsx`'s Create/Rename submit buttons now also disable on `!slugPreview` (in addition to the existing `!name.trim() || isLoading`) so the button reflects the same guard. Verified via Playwright: the Create button stays disabled for a symbol-only name and re-enables once a valid name is typed; delete has no name/slug involved and was left untouched.
- 05-prisma: `prisma/models/project.prisma` (multi-file schema, merged with `prisma/schema.prisma`'s existing `generator`/`datasource` blocks by Prisma's folder-based schema loading — `prisma.config.ts` already pointed `schema:` at the `prisma/` directory, so no schema-loading changes were needed). Added `Project` (`id` cuid, `ownerId` mapped to the Clerk user ID, `name`, optional `description`, `status` enum `ProjectStatus` (`DRAFT`/`ARCHIVED`, defaults `DRAFT`), `canvasJsonPath` (nullable — not populated until the future Vercel Blob canvas-storage unit), `createdAt`/`updatedAt`, indexes on `ownerId` and `createdAt`) and `ProjectCollaborator` (`id` cuid, `projectId` relation to `Project` with `onDelete: Cascade`, `email`, `createdAt` only — no `updatedAt`, since the spec says "creation timestamp" singular for this model — unique constraint on `[projectId, email]`, indexes on `email` and `[projectId, createdAt]`). No fields beyond what the spec lists and what Prisma requires (`@id`), per the spec's "do not add extra fields" instruction.
- 05-prisma: ran `prisma migrate dev --name init` against the `DATABASE_URL` already configured in `.env`/`.env.local` (a direct pooled Postgres connection, not `prisma+postgres://`) — applied cleanly, migration SQL at `prisma/migrations/20260810031010_init/`. Ran `prisma generate` (provider `prisma-client`, per the pre-existing `generator` block in `prisma/schema.prisma`) — output confirmed at `app/generated/prisma/` (already gitignored from a prior session), client entry point at `app/generated/prisma/client.ts`.
- 05-prisma: `lib/prisma.ts` — cached singleton, matching the file-organization rule in `code-standards.md` (`lib/` = shared infrastructure). Branches on `DATABASE_URL`'s prefix: `prisma+postgres://` constructs `new PrismaClient({ accelerateUrl: databaseUrl })` (Prisma 7's built-in Accelerate connection option — confirmed via the `PrismaClientOptionsWithAccelerate` type in `node_modules/@prisma/client/runtime/client.d.ts`, no `@prisma/extension-accelerate` package needed since it isn't in the spec's dependency list and isn't installed); anything else constructs a `PrismaPg` adapter from `@prisma/adapter-pg` and passes it as `adapter`. Cached on `globalThis` and only reused across hot reloads when `NODE_ENV !== "production"`, per spec.
- 05-prisma: verified via `prisma validate` (clean), the `migrate dev` + `generate` runs above, `tsc --noEmit` (clean), `eslint` (clean on `lib/prisma.ts`), and `npm run build` (passes — `/editor`, `/`, sign-in/up routes all still build correctly with `lib/prisma.ts` in the tree, even though nothing imports it yet).
- 06-project-apis: `app/api/projects/route.ts` — `GET` lists `Project` rows where `ownerId` equals the authenticated Clerk `userId` (`auth()` from `@clerk/nextjs/server`; explicit `401` JSON response when `userId` is missing), ordered `createdAt desc`. `POST` creates a project owned by that `userId`; missing/blank/non-string `name` defaults to `"Untitled Project"` (trimmed otherwise), per spec. IDs use the schema's existing `cuid()` default — nothing sequential was added.
- 06-project-apis: `app/api/projects/[projectId]/route.ts` — `PATCH` (rename) and `DELETE`, both behind the same `401` check, then a local `findOwnedProject(projectId, userId)` helper that returns `404` (`{error:"Project not found"}`) if the project doesn't exist and `403` (`{error:"Forbidden"}`) if `project.ownerId !== userId` — this is this unit's literal "only the project owner can rename or delete" / "non-owner mutations return 403" requirement. `PATCH` additionally 400s on an empty/whitespace-only `name` (rename has no "default to Untitled" rule — that's create-only per the spec). `params` is awaited as a `Promise<{projectId:string}>`, per this Next 16 route-handler convention (checked `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md` first, per `AGENTS.md`).
- 06-project-apis: response shapes are consistent across all four routes — success bodies are `{project}` (singular, create/rename), `{projects}` (list), or `{success:true}` (delete); errors are always `{error: string}` with the matching status code. No request-body validation library was added (none was installed and the shape is trivial: an optional `name: string`) — parsed via `request.json().catch(() => null)` plus manual `typeof` narrowing, satisfying "validate unknown external input at system boundaries" without a new dependency.
- 06-project-apis bugfix (found during verification, not in the original spec): `proxy.ts`'s existing `auth.protect()` call (from 03-auth) was intercepting unauthenticated `/api/*` requests *before* they reached these route handlers and 307-redirecting them to `/sign-in` instead of returning `401` — violating this unit's explicit "unauthenticated requests return 401" requirement. Root cause: Clerk's `auth.protect()` (`node_modules/@clerk/nextjs/dist/esm/server/protect.js`) decides redirect-vs-401/404 via an `isPageRequest()` heuristic (`Sec-Fetch-Dest`, `Accept: text/html`, or Next's internal app-router-navigation/static-store signals) that, in this Next 16 + Clerk 7 combination, evaluated `true` for *every* app-router request in dev — including plain `curl` calls to API routes with no browser-navigation headers at all (verified: `/editor` behaved identically to `/api/projects` under the same headers). Fixed at the root in `proxy.ts`: added an `isApiRoute` matcher (`/api(.*)`) that returns early from the middleware callback *before* `auth.protect()` runs, so API routes now rely solely on each route handler's own `auth()` check (already required by `code-standards.md`'s "Enforce auth and project ownership checks before any mutation") to produce the `401`. Page routes are unaffected — `/editor` still 307-redirects to `/sign-in` when unauthenticated (re-verified after the fix).
- 06-project-apis: verified via `tsc --noEmit` (clean), `eslint` (clean), `npm run build` (passes, all 4 methods across the 2 route files show as registered dynamic routes: `/api/projects`, `/api/projects/[projectId]`), and a live end-to-end pass against `next dev` — used `@clerk/backend` (already installed, a transitive dep of `@clerk/nextjs`) to create two real throwaway Clerk test users and mint session tokens, then drove the actual HTTP routes with `fetch`: unauthenticated → `401` on all 4; create with explicit name (trimmed) and with no name (→ `"Untitled Project"`) both `201`; list scoped correctly per-owner (user B's list stayed empty); owner rename `200`; non-owner rename `403`; empty-name rename `400`; rename of a nonexistent ID `404`; non-owner delete `403`; owner delete `200`; delete-again `404`. All test users/sessions/projects were deleted afterward — no leftover data.
- 07-wire-editor-home: replaced the mock data layer (`lib/mock-projects.ts`, `hooks/use-project-dialogs.ts` — both deleted) with real Prisma-backed data. `types/project.ts`'s `Project` dropped `slug` (Prisma's `Project` model has no such field; the frontend never needed it beyond the old mock) — now just `{id, name, isOwner}`.
- 07-wire-editor-home: `lib/projects.ts` (new) — `getOwnedProjects(userId)`, `getSharedProjects(email)` (matches `ProjectCollaborator.email` via `collaborators.some`), and `findAccessibleProject(projectId, userId, email)` (owner match, else a `ProjectCollaborator` lookup). This is the "existing project data helper" the spec referred to for the server-side page fetch — no such helper existed before this unit; `app/api/projects/route.ts`'s `GET` was deliberately left untouched (still queries Prisma inline) rather than refactored onto this helper, since changing a shipped/verified route's behavior wasn't required by this unit's scope (see Architecture Decisions).
- 07-wire-editor-home: `app/editor/page.tsx` is now a plain Server Component (`EditorHome` with no props) — actual data fetching and interactive chrome moved up into `app/editor/layout.tsx` (new: server component, calls `auth()` + `currentUser()`, fetches owned/shared projects via `lib/projects.ts`, renders `EditorShell`) and `components/editor/editor-shell.tsx` (new: client component owning sidebar-open state + the `useProjectActions()` hook, renders `EditorNavbar`/`ProjectSidebar`/`ProjectDialogs`, wraps `{children}`). `EditorHome` (still a client component, for its button's `onClick`) reads `openCreateDialog` via a new `ProjectActionsContext` exported from `editor-shell.tsx` instead of a prop — necessary because `EditorHome` is rendered as `{children}` from the server-component `page.tsx`, one level below where the client hook actually lives; this is the "Context providers" pattern documented in `node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md` for exactly this Server/Client composition shape.
- 07-wire-editor-home: `app/editor/[projectId]/page.tsx` (new) — the "workspace" navigation target the spec requires ("navigate to the new workspace", "redirect to /editor if deleting the active workspace"). No canvas/Liveblocks route existed yet (out of scope for this unit — see Next Up), so this is a minimal server component: `auth()` + `findAccessibleProject()` (redirects to `/editor` if the project doesn't exist or the caller is neither owner nor collaborator), then a "Canvas coming soon" placeholder showing the project's real name. Same pulled-forward-placeholder precedent as `/editor/page.tsx` in 02-editor.
- 07-wire-editor-home: `hooks/use-project-actions.ts` (new, replaces `use-project-dialogs.ts`) — same dialog-state shape as before but mutations now call the real API instead of mutating local mock state. `submitCreate`: `POST /api/projects`, then `router.push` to the new `/editor/{project.id}` workspace *and* `router.refresh()` — the `refresh()` is required, not optional, because `app/editor/layout.tsx`'s data fetch does not automatically re-run on a client-side navigation to a sibling route under the same layout (confirmed via live testing: without it, the sidebar kept showing stale/empty data right after creating a project — Next.js's Router Cache reuses the already-rendered layout output across navigations within the same layout unless explicitly refreshed). `submitRename`: `PATCH /api/projects/{id}`, then `router.refresh()`. `submitDelete`: `DELETE /api/projects/{id}`, then compares `usePathname()` against `/editor/{id}` to decide `router.push("/editor")` (active workspace) vs. `router.refresh()` (not active) — matches the spec's two branches exactly.
- 07-wire-editor-home: create dialog's "generate a short unique suffix" + "slugify the name to create the room ID" are implemented as a cosmetic preview only (`roomIdPreview` in the hook: `${slugify(name)}-${shortSuffix}`, suffix regenerated each time the dialog opens via `crypto.randomUUID().slice(0,6)`), shown in the dialog as "Room ID: {preview}". This is *not* what gets sent anywhere — the actual project ID (and future Liveblocks room ID) comes from Prisma's `cuid()` in the `POST /api/projects` response. Flagged under Next Up: when Liveblocks is wired up, the room ID must be `project.id`, not this preview string — that's what "the project ID and Liveblocks room ID should stay aligned" means.
- 07-wire-editor-home: verified via `tsc --noEmit` (clean, after running `next typegen` once to generate the new `/editor` and `/editor/[projectId]` route's `LayoutProps`/`PageProps` helper types), `eslint .` (clean), `npm run build` (passes; `/editor` and `/editor/[projectId]` both show as registered dynamic routes), and a live end-to-end pass against `next dev` using the same `@clerk/backend` two-test-user pattern as 06, this time driven through actual browser pages (Playwright, `Authorization: Bearer <session token>` header authenticates SSR page requests the same way it authenticates API routes) rather than raw `fetch`: confirmed real (empty) project data on first load; create dialog's room-ID preview renders and updates live; create navigates to `/editor/{id}` showing the real project name and the sidebar picks up the new project; rename updates the sidebar in place and stays on the workspace URL; a project shared via a seeded `ProjectCollaborator` row appeared under user B's "Shared" tab with no rename/delete buttons (owner-only actions confirmed absent for non-owners); deleting the active workspace redirected to `/editor`; deleting a non-active project left the user in place and refreshed the sidebar. All seeded test data (users, sessions, projects, collaborator rows) were cleaned up afterward.
- 08-editor-workspace-shell: renamed the dynamic segment from `app/editor/[projectId]/` to `app/editor/[roomId]/` per the spec's literal route name (`/editor/[roomId]`) — the URL is unchanged (still `/editor/{project.id}`, since the project ID and future Liveblocks room ID are the same value per 07's decision), only the internal param name changed.
- 08-editor-workspace-shell: `lib/project-access.ts` (new) — `getCurrentIdentity()` (wraps `auth()` + `currentUser()` into `{userId, email} | null`, replacing the duplicated inline version previously in both `app/editor/layout.tsx` and the workspace page) and `checkProjectAccess(projectId, userId, email)` (thin wrapper over `lib/projects.ts`'s existing `findAccessibleProject`, kept as a single query implementation rather than duplicating the Prisma lookup under a second name).
- 08-editor-workspace-shell: `components/editor/access-denied.tsx` (new) — standalone full-viewport component (lock icon, message, link back to `/editor`), rendered directly by the workspace page (not wrapped in any editor chrome) when `checkProjectAccess` returns null, for both "authenticated but no access" and "project doesn't exist" cases. This replaces 07's behavior of silently `redirect("/editor")` on the same two cases, per this spec's explicit "see AccessDenied" requirement.
- 08-editor-workspace-shell: restructured `app/editor/` into a route group so the workspace room gets its own full-viewport shell instead of nesting inside the generic editor-home chrome: `app/editor/page.tsx` and `app/editor/layout.tsx` moved to `app/editor/(home)/` (URL unaffected — route groups are stripped from the path), leaving `app/editor/[roomId]/page.tsx` as a sibling that is no longer wrapped by `EditorShell`/`EditorNavbar`. This was necessary because the spec's workspace navbar (project name, share button, AI toggle) is structurally different from the generic home navbar (sidebar toggle, `UserButton` only) — nesting one inside the other would have produced two stacked navbars. See Architecture Decisions.
- 08-editor-workspace-shell: `components/editor/workspace-navbar.tsx` (new) and `components/editor/workspace-shell.tsx` (new, client component) — the room-specific top navbar (sidebar toggle, centered project name, inert Share button, working AI-sidebar toggle, `UserButton`) and the full-viewport shell that composes it with the existing `ProjectSidebar`, a centered canvas placeholder (`bg-page`, fills remaining space), and a right-side AI placeholder panel that mounts/unmounts with the AI toggle. `WorkspaceShell` fetches and owns its own `ownedProjects`/`sharedProjects` (via the same `lib/projects.ts` helpers `(home)`'s layout uses) and its own `useProjectActions()` instance, since it's no longer inside the shared `EditorShell` — create/rename/delete from within a room still work (`submitDelete`'s existing pathname check already redirects to `/editor` when the active room is deleted).
- 08-editor-workspace-shell: `components/editor/project-sidebar.tsx` extended with an optional `activeProjectId` prop — `ProjectListItem` highlights (background + primary/medium text) the row matching the current room, satisfying "current room highlighted in the sidebar." `(home)`'s `EditorShell` doesn't pass this prop (no active room on the home screen); `WorkspaceShell` passes the current `roomId`.
- 08-editor-workspace-shell: verified via `tsc --noEmit` (clean, after `next typegen`), `eslint .` (clean), `npm run build` (passes; `/editor` and `/editor/[roomId]` both registered), and a live end-to-end Playwright pass against `next dev` (same `@clerk/backend` throwaway-user pattern as 06/07 — owner, collaborator, and unrelated-stranger test users): unauthenticated request to `/editor/{roomId}` redirected to `/sign-in`; owner saw the project name in the navbar, the canvas placeholder, the Share button, and the AI toggle; clicking the AI toggle revealed the right placeholder panel; opening the sidebar showed the current room highlighted; a stranger with no owner/collaborator relationship saw `AccessDenied` with a working "Back to Editor" link; a nonexistent project ID also showed `AccessDenied`; the seeded collaborator saw the shared project's workspace render correctly. All test users/sessions/projects were cleaned up afterward. Restarting the long-running dev server was required mid-verification — its route manifest, cached from before the `[projectId]`→`[roomId]` rename, threw "You cannot use different slug names for the same dynamic path" until `.next` was cleared and the server restarted; this is expected Next.js dev-cache behavior after a dynamic-segment rename, not an app bug.

- 08-editor-workspace-shell UI pass: redesigned the workspace shell's visual style to match a supplied reference screenshot (previously it was a bare "Canvas coming soon." line). `components/editor/workspace-navbar.tsx` now shows a project-name/"Workspace" title stack on the left and pill-style Share/AI action buttons (branded `bg-brand` for AI) on the right, replacing the earlier icon-only buttons. `components/editor/workspace-shell.tsx`'s `CanvasPlaceholder` gained a radial brand-colored glow, a bordered compass icon, an uppercase "Workspace Shell" label, and descriptive heading/paragraph copy; `AiSidebarPlaceholder` gained an "AI Copilot" header and two content cards ("Chat surface pending", "Future Hooks"). `components/editor/project-sidebar.tsx`'s active-project row now shows a small brand-colored dot plus a brand-tinted highlight (was plain `bg-subtle`), and "New Project" is now a solid brand-colored pill button.
- 08-editor-workspace-shell UI pass (follow-up): removed the grid-line background from `CanvasPlaceholder` (kept the radial glow) and made the canvas reflow with the left `ProjectSidebar` instead of sitting statically underneath it — `CanvasPlaceholder` now takes an `isSidebarOpen` prop and applies `lg:ml-[19.5rem]` (matching the sidebar's `left-3` + `w-72` + gap) with a `transition-[margin-left] duration-200 ease-out` that mirrors the sidebar's own slide transition, so the canvas visibly shrinks/grows and its centered content re-centers as the sidebar opens/closes. Scoped to `lg:` only, matching the sidebar's existing desktop-vs-mobile split (mobile keeps the dimmed-backdrop overlay behavior from 08, where shifting canvas content on a narrow viewport wouldn't leave useful room). Verified visually via two Playwright screenshots (sidebar closed vs. open) against a live authenticated session.

- 09-share-dialog: `types/collaborator.ts` (`Collaborator { id, email, name, avatarUrl }`, `name`/`avatarUrl` nullable) and `lib/collaborators.ts` (new) — `getCollaborators(projectId)`, `addCollaborator(projectId, email)`, `removeCollaborator(projectId, collaboratorId)`. Enrichment is a private `enrichCollaborators()` helper: one batched `clerkClient().users.getUserList({ emailAddress: [...] })` call per list (not one Clerk API call per collaborator), matched back to `ProjectCollaborator` rows by scanning each returned Clerk user's `emailAddresses` array. Falls back to `{name: null, avatarUrl: null}` (UI then displays the bare email) when no Clerk user matches, per spec.
- 09-share-dialog: `lib/project-access.ts` gained an exported `findOwnedProject(projectId, userId)` — the same `{project} | {error: NextResponse}` shape already used locally inside `app/api/projects/[projectId]/route.ts` (06-project-apis), now shared so the two new owner-gated collaborator routes don't reimplement the 404-vs-403 check a third time. The original local copy in `[projectId]/route.ts` was left as-is (already shipped/verified, not part of this unit's scope).
- 09-share-dialog: `app/api/projects/[projectId]/collaborators/route.ts` — `GET` (list, any accessor: owner or collaborator, via the existing `checkProjectAccess`) and `POST` (invite, owner-only via the new `findOwnedProject`). `POST` validates the email with a simple regex, lowercases/trims it, and pre-checks for an existing `[projectId, email]` row to return `409 "Already a collaborator"` instead of relying on catching the DB's unique-constraint error. `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts` — `DELETE` (remove, owner-only), scoped by both `id` and `projectId` in one `deleteMany` so a collaborator ID from a different project can't be removed cross-project; returns `404` if nothing matched.
- 09-share-dialog: `components/editor/share-dialog.tsx` (new) — a `Dialog` (shadcn) rendered by `WorkspaceShell`, opened via a new `onOpenShare` prop on `WorkspaceNavbar` wired to the existing (previously inert, per 08's Next Up note) Share button. Owner view (`isOwner` prop, computed server-side in `app/editor/[roomId]/page.tsx` as `project.ownerId === identity.userId`) shows the invite form and per-row remove buttons; non-owners see the same collaborator list with neither. Each row shows a Clerk avatar `<img>` (or a generic `UserRound` icon fallback) plus `name ?? email`, with the raw email as a secondary line only when a name is present (so an unenriched row doesn't show the same email twice). Copy-link uses `navigator.clipboard.writeText` and flips the button to "Copied!" for 2s.
- 09-share-dialog: `hooks/use-share-dialog.ts` (new) — deliberately holds no local `collaborators` array (unlike the mock-era `use-project-dialogs.ts`'s in-memory list). `invite()`/`remove()` call the API then `router.refresh()`, the same pattern `use-project-actions.ts` established for rename/delete — the collaborator list itself always comes from the `collaborators` prop threaded down from the server-fetched `app/editor/[roomId]/page.tsx`, so a refresh is both necessary and sufficient to reflect a mutation; no client-side cache to keep in sync.
- 09-share-dialog: verified via `tsc --noEmit` (clean, after `next typegen` for the two new API route files), `eslint .` (clean), `npm run build` (passes; both new collaborator routes registered), and a live Playwright pass against `next dev` using the `@clerk/backend` throwaway-user pattern (owner + a second real Clerk user as the invited collaborator): non-owner `POST`/`DELETE` on the collaborators endpoints both `403`'d; owner's share dialog showed the invite form and an empty-state message before inviting; after inviting the collaborator's real email, the row rendered with their actual Clerk-enriched display name and avatar image (not just the email); "Copy link" flipped to "Copied!"; the collaborator's own view of the same dialog showed the list with no invite input and no remove buttons; owner then removed the collaborator and the row disappeared, empty-state message returned. (First run of this check showed the invited row not appearing — a 500ms wait after clicking Invite wasn't enough for the `router.refresh()` round-trip to land; re-verified with a longer wait and it appeared correctly, confirming it was a test-timing artifact, not an app bug.)

- 10-liveblocks-setup: `liveblocks.config.ts` (already present as an unfilled template at the project root, untracked in git — not created by a prior unit) had its `Presence` (`cursor: {x,y} | null`, `isThinking: boolean`) and `UserMeta` (`id`, `info: {name, avatar, color}`) filled in per spec. The template's other sections (`Storage`, `RoomEvent`, `ThreadMetadata`, `RoomInfo`) were out of this unit's scope, left as empty placeholders — only changed their type from a lint-failing `{}` to `Record<string, never>` (see bugfix note below), no fields added.
- 10-liveblocks-setup: `lib/liveblocks.ts` (new) — `getLiveblocksClient()`, a lazily-constructed singleton cached on `globalThis` (same cross-hot-reload caching pattern as `lib/prisma.ts`, but exposed as a getter function rather than an eagerly-evaluated `export const`; see bugfix note below for why), plus `getCursorColor(userId)`, a pure deterministic string-hash → fixed 9-color-palette mapping (no randomness, no external calls — same `userId` always yields the same color).
- 10-liveblocks-setup: `app/api/liveblocks-auth/route.ts` (new) — `POST` handler: `401` via `getCurrentIdentity()` (existing helper, `lib/project-access.ts`) if unauthenticated; `400` if the request body's `room` field (the Liveblocks client sends `{room: roomId}`, confirmed via the SDK's `auth-endpoint-callback` docs) is missing/non-string; `403` via the existing `checkProjectAccess()` helper (owner-or-collaborator check, already built in 08-editor-workspace-shell) if the caller can't access that project — the project ID is used directly as the Liveblocks room ID, no separate mapping. On success: `liveblocks.getOrCreateRoom(roomId, {defaultAccesses: []})` (idempotent — creates only if missing, private by default), then `prepareSession()` + `session.allow(roomId, session.FULL_ACCESS)` with `userInfo: {name, avatar, color}` sourced from Clerk (`name`/`avatar` — see next note) and `getCursorColor(userId)`, then `session.authorize()`'s `{status, body}` returned directly as the response Liveblocks expects.
- 10-liveblocks-setup: `lib/project-access.ts`'s `ClerkIdentity`/`getCurrentIdentity()` (existing helper, 08-editor-workspace-shell) extended with `name`/`avatarUrl` fields (from Clerk's `user.fullName`/`user.imageUrl` — the same fields `lib/collaborators.ts`'s `enrichCollaborators()` already reads, just sourced from the already-fetched `currentUser()` call instead of a second Clerk API lookup) since the auth route needed real user name/avatar and this helper already had the one `currentUser()` call needed to get them. Purely additive — existing callers (`app/editor/(home)/layout.tsx`, `app/editor/[roomId]/page.tsx`, the two collaborator routes) are unaffected.
- 10-liveblocks-setup bugfix (found during verification): `new Liveblocks({secret})` validates the secret key's format (`sk_...` prefix) synchronously in its constructor. Constructing it eagerly at module scope (the pattern used in `lib/prisma.ts`, since `DATABASE_URL` has no such eager validation) made `npm run build` fail outright during Next's page-data-collection step for `/api/liveblocks-auth`, with *any* placeholder/empty key — not just an invalid-but-present one. Fixed by making `lib/liveblocks.ts`'s client construction lazy (a `getLiveblocksClient()` getter, only invoked from inside the route handler) instead of an eagerly-evaluated module-scope `export const`; caching on `globalThis` is preserved. This is a real constraint for any future unit touching this file, not just a one-off workaround.
- 10-liveblocks-setup bugfix: `liveblocks.config.ts`'s pre-existing template used `{}` (the TypeScript "empty object" type, which actually allows any non-nullish value) for `RoomEvent`/`ThreadMetadata`/`RoomInfo`/`Storage`'s placeholder types — `eslint`'s `@typescript-eslint/no-empty-object-type` rule failed on all four. Changed to `Record<string, never>` (correctly means "no properties"), scoped to only that type-safety fix — no fields added to any of them, since none were in this unit's spec.
- 10-liveblocks-setup — **environment gap, not yet resolved**: `@liveblocks/node` was not actually installed despite `10-liveblocks-setup.md` stating "All required Liveblocks packages are already installed" (only `@liveblocks/client`, `@liveblocks/react`, `@liveblocks/react-flow`, `@liveblocks/react-ui` were present) — installed it (`^3.23.1`) this session. `LIVEBLOCKS_SECRET_KEY` was also never set anywhere (`.env`, `.env.local`); added a placeholder empty entry to `.env.local` with a `# TODO` comment pointing at the Liveblocks dashboard. **The auth route will 500 at runtime until a real key is pasted in** — `npm run build` passes regardless (see bugfix above) but this was not end-to-end verified against a live Liveblocks project/room in this session, unlike every other unit's Session Notes pattern of a full Playwright/Clerk pass. Do that live verification once a real key is in place, before trusting this route in production.

- 11-base-canvas: `types/canvas.ts` (new) — `NodeShape` (6 literal values: `rectangle`/`diamond`/`circle`/`pill`/`cylinder`/`hexagon`) + `NODE_SHAPES` array, `NodeColor` interface (`fill`/`text`) + `NODE_COLORS` array (the 8 fill/text hex pairs from `ui-context.md`'s Canvas section, in the same order, first entry doubling as the documented default) + `DEFAULT_NODE_COLOR`, `CanvasNodeData` (`{label, color, shape}` — exactly the three fields the spec asked for, no more), and the two custom-type aliases the spec named explicitly: `CanvasNode = Node<CanvasNodeData, "canvasNode">` and `CanvasEdge = Edge<Record<string, never>, "canvasEdge">` (edges carry no custom data yet — the spec's "Node data should support" list is node-only). Defined as `type`, not `interface`, for `CanvasNodeData` — `@xyflow/react`'s `Node<NodeData extends Record<string, unknown>>` constraint is satisfied structurally by a type-literal but not reliably by a bare interface.
- 11-base-canvas: `components/editor/canvas.tsx` (new, client) — the actual `<ReactFlow>` render. Calls `useLiveblocksFlow<CanvasNode, CanvasEdge>({ suspense: true, nodes: {initial: []}, edges: {initial: []} })` (suspense mode per spec — this is why it must be rendered inside a `ClientSideSuspense` boundary, not directly) and passes the returned `nodes`/`edges`/`onNodesChange`/`onEdgesChange`/`onConnect`/`onDelete` straight into `ReactFlow`. Render props per spec: `connectionMode={ConnectionMode.Loose}` ("loose connection behavior" — allows connecting from any handle, not just source→target), `fitView`, a `<Background variant={BackgroundVariant.Dots} />`, and a `<MiniMap />`. No `nodeTypes`/`edgeTypes` map registered and no `<Controls />` — both explicitly out of scope ("don't add controls yet", "don't add custom node or edge rendering yet"); since nodes/edges start empty there's nothing that would try to render under the unregistered `"canvasNode"`/`"canvasEdge"` type strings yet. Imports `@xyflow/react/dist/style.css` directly in this file (the only consumer of React Flow in the app so far).
- 11-base-canvas: `components/editor/canvas-room.tsx` (new, client) — the "client-side editor/canvas wrapper" the spec asked for. `LiveblocksProvider authEndpoint="/api/liveblocks-auth"` → `RoomProvider id={roomId} initialPresence={{cursor: null, isThinking: false}}` (spec only specified `cursor: null`; `isThinking` was added to satisfy `Presence`'s full shape from `liveblocks.config.ts`, defaulted `false` since the spec gives no other guidance) → a new `RoomConnectionGuard` component → `ErrorBoundary` (from the newly-added `react-error-boundary` dep) → `ClientSideSuspense fallback={<CanvasLoading />}` → `Canvas`. `RoomConnectionGuard` calls Liveblocks' `useErrorListener` and swaps in `CanvasConnectionError` when `error.context.type === "ROOM_CONNECTION_ERROR"` fires — this, not the `ErrorBoundary`, is what actually satisfies the spec's "error fallback for Liveblocks connection issues" bullet; the `ErrorBoundary` alone only catches errors thrown during React's render (e.g. a bug in `Canvas` itself), and Liveblocks connection/auth failures do not throw into the render tree, they surface only via `useErrorListener` (confirmed both from the `liveblocks-best-practices` skill's `handling-connection-errors` reference and by direct observation — see Session Notes). Kept the `ErrorBoundary` too, as defense-in-depth for genuine render errors, matching the skill's general `ClientSideSuspense`+`ErrorBoundary` composition pattern.
- 11-base-canvas: `components/editor/workspace-shell.tsx` — `CanvasPlaceholder` (08's placeholder copy/icon/glow) replaced by a slimmer `CanvasArea` that keeps the same sidebar-reflow wrapper (`lg:ml-[19.5rem]` transition, unchanged from 08) but renders `<CanvasRoom roomId={roomId} />` instead of static copy. No other prop/behavior changes to `WorkspaceShell`.
- 11-base-canvas: installed `react-error-boundary` (new dependency) — needed for the `ErrorBoundary` component used in the wrapper above; not previously in the project despite several other Liveblocks packages already being installed. `@xyflow/react` and all `@liveblocks/*` packages needed were already present (confirmed before writing any code) except this one.
- 11-base-canvas: verified via `tsc --noEmit` (clean, after `next typegen`), `eslint` (clean on all new/changed files), and `npm run build` (passes, `/editor/[roomId]` still registers correctly). Live verification was partial, blocked by the same pre-existing gap 10-liveblocks-setup flagged and left unresolved: `LIVEBLOCKS_SECRET_KEY` is still an empty placeholder in `.env.local`, so `/api/liveblocks-auth` 500s on every request (a real, already-known environment gap — not something this unit introduced or could fix without a real key from the Liveblocks dashboard). Confirmed via a live Playwright pass (`@clerk/backend` throwaway-user pattern, same as prior units): the workspace page renders correctly with the new `CanvasArea`/`CanvasRoom` in place of the old placeholder, `RoomProvider` correctly attempts the room connection (repeated `POST /api/liveblocks-auth` calls visible in server logs), and the `ClientSideSuspense` fallback (`CanvasLoading`, "Loading canvas…", correctly styled with `bg-page`/`text-copy-muted` tokens) renders with no app-level crash. Could **not** confirm the full happy path (real synced nodes/edges, `MiniMap`, dot background actually painting) or the `useErrorListener`→`CanvasConnectionError` fallback actually firing, because the malformed 500 this specific gap produces (an uncaught exception in `getLiveblocksClient()`, not a clean Liveblocks-shaped auth rejection) makes the client's auth manager retry indefinitely rather than resolve to a definitive `ROOM_CONNECTION_ERROR` — by design, transient-looking failures are retried, not surfaced. **Once a real `LIVEBLOCKS_SECRET_KEY` is in place, re-verify both of those paths** — same outstanding item 10-liveblocks-setup already left open, now with two more things riding on it.

- 12-shape-panel: `types/canvas.ts` extended (not replaced) with `NodeSize` (`{width, height}`), `NODE_DEFAULT_SIZES: Record<NodeShape, NodeSize>` (per spec's relative rules: rectangle wider-than-tall `160×80`, circle square `100×100`, diamond larger-for-label-room `170×170`, plus sensible sizes for pill/cylinder/hexagon not otherwise specified by the spec), `SHAPE_DRAG_MIME_TYPE` (`"application/x-ghost-shape"`, the `DataTransfer` key both the toolbar and the canvas drop handler agree on), `ShapeDragPayload` (`{shape, width, height}` — the literal drag-payload contents this unit's spec asks for), and `getNodeColorByFill()` (looks up the `{fill,text}` pair from `NODE_COLORS` matching a node's stored `color` string, falling back to `DEFAULT_NODE_COLOR` — needed because `CanvasNodeData.color` was already typed as a bare `string` by 11-base-canvas, so the renderer needs a way back to the paired text color).
- 12-shape-panel: `components/editor/shape-toolbar.tsx` (new) — the floating pill-shaped toolbar (`rounded-full`, `bg-elevated/95` + `backdrop-blur-sm`, positioned `absolute bottom-6 left-1/2 -translate-x-1/2`). Renders one `draggable` icon button per `NODE_SHAPES` entry (lucide `RectangleHorizontal`/`Diamond`/`Circle`/`Pill`/`Cylinder`/`Hexagon` — chosen because they're the closest stroke-icon match to each shape name, `Pill` doubling as a literal name match). `onDragStart` writes a `ShapeDragPayload` (shape + that shape's `NODE_DEFAULT_SIZES` entry) into the event's `DataTransfer` under `SHAPE_DRAG_MIME_TYPE`, and sets `effectAllowed = "move"`.
- 12-shape-panel: `components/editor/canvas-node.tsx` (new) — `CanvasNodeRenderer`, the "basic renderer for the custom canvas node type" the spec asks for: a single bordered `div` filling the node's `width`/`height`, centered label text, background = `data.color`, border/text = the paired text color from `getNodeColorByFill(data.color)`. Every shape renders identically (a rectangle) per the spec's explicit "shape-specific visuals will be added later" — no per-shape SVG yet, and no connection `Handle`s yet either (none existed before this unit, and none were named in this unit's Implementation steps).
- 12-shape-panel: `components/editor/canvas.tsx` restructured — `Canvas` is now a thin `<ReactFlowProvider>` wrapper around a new inner `FlowCanvas` component, because generating canvas coordinates from a screen drop point requires `useReactFlow().screenToFlowPosition()`, which only works below a `ReactFlowProvider` (this is the standard xyflow drag-and-drop composition shape: the component calling `useReactFlow()` must be a sibling-under-provider of `<ReactFlow>`, not a plain ancestor with no provider at all — `<ReactFlow>` alone does create an implicit provider, but only visible to its own children, not to the component rendering it). `FlowCanvas` wraps `<ReactFlow>` in a `relative h-full w-full` div carrying `onDragOver` (calls `preventDefault()` + sets `dropEffect = "move"`, required by the HTML5 DnD spec for `drop` to fire at all) and `onDrop`. `nodeTypes={{canvasNode: CanvasNodeRenderer}}` is now registered (module-level constant, not recreated per render — an unstable `nodeTypes` object triggers an xyflow console warning and unnecessary remounts). `ShapeToolbar` renders as an absolutely-positioned sibling of `<ReactFlow>` inside the same wrapper div.
- 12-shape-panel: on `drop`, `FlowCanvas` reads the `ShapeDragPayload` back out of `DataTransfer`, converts the event's `clientX`/`clientY` via `screenToFlowPosition`, builds a `CanvasNode` (`type: "canvasNode"`, `width`/`height` from the payload, `data: {label: "", color: DEFAULT_NODE_COLOR.fill, shape: payload.shape}`), and dispatches it with `onNodesChange([{type: "add", item: newNode}])` — `useLiveblocksFlow` exposes no direct `setNodes`/`addNode`, but its `onNodesChange` accepts the same `NodeChange[]` union `@xyflow/react` itself defines, which includes a `{type: "add", item}` variant for exactly this case, so no extra state layer was needed. Node IDs are `` `${shape}-${Date.now()}-${counter}` `` per spec, with `counter` a `useRef` that persists (and increments) across drops within a session — not reset per-drop, so two nodes dropped in the same millisecond still get distinct IDs.
- 12-shape-panel: verified via `tsc --noEmit` (clean), `eslint` (clean on all new/changed files), `npm run build` (passes), and a live Playwright pass against `next dev` using the established `@clerk/backend` throwaway-user pattern. This session found `LIVEBLOCKS_SECRET_KEY` in `.env.local` is now a real key (no longer the empty placeholder 10-/11-base-canvas left open) — so this was also the first unit able to confirm the canvas's full happy path end-to-end: the room connects for real (no more indefinite-retry loading state), the shape toolbar renders all 6 buttons, and — since real OS-level drag-and-drop isn't reliably triggerable through Playwright/CDP in a headless context — the drag/drop sequence was verified by dispatching real `dragstart`/`dragover`/`drop` `DragEvent`s with a constructed `DataTransfer` directly in the page (exercises the actual `onDragStart`/`onDragOver`/`onDrop` handlers, not a mocked substitute). Confirmed: dropped a Rectangle → node appears at `160×80`; dropped a Circle → node appears at `100×100` (square, confirming per-shape sizing); both rendered as `.react-flow__node-canvasNode` (the custom type registered correctly) with empty label text, `rgb(31,31,31)` background / `rgb(237,237,237)` border (exactly `DEFAULT_NODE_COLOR`'s fill/text hex), and IDs `rectangle-<ts>-0` / `circle-<ts>-1` (shape+timestamp+incrementing-counter, as specified). Screenshot confirmed the pill toolbar visually bottom-center with 6 icons, both dropped nodes as bordered rounded rectangles, MiniMap in the corner. The only console errors were the already-documented Bearer-token-vs-Clerk-CDN CORS artifact (see Session Notes in this file, `08-editor-workspace-shell`/`11-base-canvas` era) — not an app defect. Test project rows were cleaned up directly via Prisma (`deleteMany` by name) since the throwaway Clerk user was deleted first, same as always, but the app's `ownerId` field has no DB-level FK to Clerk users, so this cleanup step doesn't cascade automatically — noted here in case a future session assumes it does.
- 12-shape-panel follow-up fix (prompted by "check Liveblocks best practices"): read `@liveblocks/react-flow`'s actual source (`node_modules/@liveblocks/react-flow/dist/lib/flow.js`, since the skill's own `multiplayer-react-flow` reference and the public docs don't cover dynamic node-add patterns) to confirm `onNodesChange([{type:"add", item}])` — the pattern this unit already used — really is the correct, sanctioned way to add a node: it's a `useMutation` under the hood, and its `applyNodeChanges`'s `"add"` case does `nodes.get(item.id) ? existing.reconcile(item, config) : nodes.set(item.id, ...)` against the room's shared `LiveMap`, keyed by the node's `id`. That confirmed the architecture was right but surfaced a real multiplayer-specific bug in the *ID generation*, not the add mechanism: the spec's literal `${shape}-${timestamp}-${counter}` scheme used a `useRef` counter starting at `0` in every browser tab. Two different users each dropping their first shape of a session within the same millisecond (a realistic scenario in a live multiplayer canvas, e.g. two people clicking at once) would generate the *same* ID, and the `reconcile`-else-`set` logic above means the second drop would silently merge into / overwrite the first instead of creating a second node — a real data-loss bug specific to this being a realtime multiplayer app, not something a single-user React Flow tutorial would ever surface. Fixed in `components/editor/canvas.tsx` by seeding the counter from `Math.floor(Math.random() * 1_000_000)` instead of `0`, lazily on the first actual drop (inside `handleDrop`, not render) rather than eagerly in the component body — an eager `useRef(Math.random())` call tripped this project's `react-hooks/purity` ESLint rule ("Cannot call impure function during render"), so the ref starts `null` and is seeded on first use instead. This keeps the spec's literal 3-part ID format (shape, timestamp, counter) intact while making cross-client collisions astronomically less likely instead of guaranteed-on-first-drop.
- 12-shape-panel follow-up fix, second finding: `handleDragOver` previously called `preventDefault()` and force-set `dropEffect = "move"` for *any* drag entering the canvas, not just shape-toolbar drags — meaning dragging an unrelated item (a browser link, an image, a future drag source) over the canvas would show a misleading "drop allowed" cursor even though `handleDrop`'s `if (!raw) return` guard meant nothing would actually happen. Fixed by checking `event.dataTransfer.types.includes(SHAPE_DRAG_MIME_TYPE)` in `handleDragOver` before calling `preventDefault()`, so only real shape drags claim the drop zone; also moved `handleDrop`'s `preventDefault()` to after its own payload check for the same reason (a stray non-shape drop that somehow still fires `drop` now falls through to the browser's own default handling instead of being silently swallowed).
- Both fixes verified live (same `@clerk/backend` + dispatched-`DragEvent` Playwright technique as the original verification): dispatching a `dragover` with a non-shape `DataTransfer` (plain text) against the canvas pane confirmed the event is no longer cancelled (`preventDefault` no longer called); two same-shape (`Hexagon`) drops in the same session produced distinct IDs (`hexagon-<ts1>-287124`, `hexagon-<ts2>-287125`) confirming the random-seeded counter still increments correctly per spec's format. Test project and temp scripts cleaned up the same way as the original unit's verification.

- 13-node-shape: `types/canvas.ts` extended (not replaced) with shared SVG geometry constants — `DIAMOND_POINTS`, `HEXAGON_POINTS` (polygon point lists), `CYLINDER_BODY_PATH` (an SVG path arcing the top/bottom into the body), `CYLINDER_TOP_ELLIPSE` (the lid overlay) — all defined on a `0 0 100 100` viewBox so they can be stretched to any node's actual `width`/`height` via `preserveAspectRatio="none"`. Kept in `types/canvas.ts` (not a new `lib/` file) since that's already the single source of truth for every other shape-related constant (`NODE_SHAPES`, `NODE_DEFAULT_SIZES`, `NODE_COLORS`) and both consumers below need the exact same numbers, not just the same shapes.
- 13-node-shape: `components/editor/canvas-node.tsx` rewritten — `CanvasNodeRenderer` now branches on `data.shape`. `rectangle`/`pill`/`circle` render as a single styled `div` (`rounded-xl` for rectangle, `rounded-full` for pill/circle — `rounded-full` on a non-square pill-sized box naturally produces the stadium/pill shape, no separate CSS needed). `diamond`/`hexagon`/`cylinder` render an inline `<svg>` (`ShapeSvg`, polygon for diamond/hexagon, path+ellipse for cylinder's body+lid) sized to `h-full w-full` so it scales with the node's actual on-canvas size, with the label as a separate absolutely-positioned `div` overlaid on top (SVG `<text>` was avoided — a plain HTML label div matches the CSS-shape branch's text rendering exactly and is simpler to center/wrap). Borders satisfy "subtle at rest, brighter when selected" via the new `selected` prop React Flow already passes to every custom node type (`NodeProps.selected`, confirmed via `@xyflow/react`'s type defs — no change needed to how `canvas.tsx` invokes the renderer): unselected border/stroke color is the node's paired accent text color with a `66` hex alpha suffix (~40% opacity) and `1px`/`1.5` width; selected is the same color at full opacity and `2px`/`3` width.
- 13-node-shape: `components/editor/shape-toolbar.tsx` — added a cursor-attached ghost preview during shape drags, using the browser's native `dataTransfer.setDragImage()` (not manual mouse-position tracking) so the preview automatically follows the cursor and automatically disappears the instant the drag ends, whether by drop or Escape/cancel — no extra state or cleanup-on-cancel logic needed to satisfy that requirement. `createDragPreviewElement(shape)` builds a small off-screen (`position:fixed; top:-1000px`) DOM node — vanilla `document.createElement`/`createElementNS`, not a React render — sized to that shape's `NODE_DEFAULT_SIZES` entry and using `DEFAULT_NODE_COLOR` (matching "same shape type and default size that will be used on drop," since dropped nodes always start with the default color per 12-shape-panel), built from the same shared geometry constants `canvas-node.tsx` uses so the preview can't visually drift from the real node. Plain DOM (not a React `createRoot` into a detached container) was chosen deliberately: `onDragStart` fires inside React's synthetic event handling, and `setDragImage` needs the element already painted with real layout at the moment it's called — relying on a second React root committing synchronously inside that same event was an unnecessary risk for a few lines of `createElementNS` calls. The element is removed via `setTimeout(..., 0)` right after `setDragImage()` — the browser has already snapshotted it by then, matching the standard drag-image-cleanup pattern.
- 13-node-shape: verified via `tsc --noEmit` (clean), `eslint` (clean on all changed files), `npm run build` (passes), and a live Playwright pass against the already-running `next dev` server (port 3000) using the established `@clerk/backend` throwaway-user pattern: dispatched real `dragstart`/`dragover`/`drop` `DragEvent`s (same technique 12-shape-panel established — headless CDP can't drive genuine OS-level HTML5 drag-and-drop) to drop all 6 shapes onto the canvas. Confirmed: all 6 render (`.react-flow__node-canvasNode` count 6), exactly 3 contain an `<svg>` (diamond/hexagon/cylinder) and 3 don't (rectangle/pill/circle, which instead show `rounded-xl`/fully-rounded computed `border-radius`); screenshot confirms correct silhouettes for every shape including the cylinder's lid ellipse; clicking a node changed its computed `border-color` from `rgba(237,237,237,0.4)` to solid `rgb(237,237,237)` (the selected-vs-unselected brightness requirement, confirmed numerically not just visually). Also directly verified the drag-preview mechanism (real OS drag-image capture isn't observable via synthetic `DragEvent`s in headless CDP, so this checked the underlying calls instead): for all 6 shapes, `dataTransfer.setDragImage()` was called without throwing, with an element whose inline width matched that shape's `NODE_DEFAULT_SIZES` and whose SVG-vs-plain-div makeup matched the real renderer, and zero preview elements were left in the DOM 50ms later (confirming the `setTimeout(0)` cleanup ran). Test project and Clerk user were deleted afterward; the temporary script and scratchpad Playwright symlink used for this run were both removed.

- 14-node-editing: `types/canvas.ts` gained `NODE_MIN_SIZE` (`{width: 48, height: 32}`) — the resize floor, chosen smaller than every shape's `NODE_DEFAULT_SIZES` entry so it's a real constraint without being tighter than any shape ever needs by default.
- 14-node-editing: `components/editor/canvas-node.tsx` (`CanvasNodeRenderer`) rewritten to add resizing and inline label editing on top of 13-node-shape's per-shape renderer, with no changes to shape rendering itself. Resizing: renders `@xyflow/react`'s built-in `<NodeResizer isVisible={selected} minWidth={NODE_MIN_SIZE.width} minHeight={NODE_MIN_SIZE.height} .../>` (no `nodeId` prop needed — it resolves the current node via `useNodeId()` context automatically), styled subtly via `handleStyle`/`lineStyle` inline styles referencing the existing `--border-subtle`/`--bg-elevated` CSS tokens (not hardcoded hex) rather than the library's default white-bordered handles. No manual wiring to Liveblocks was needed: confirmed by reading `node_modules/@xyflow/react`'s source that `NodeResizer` dispatches its size changes via the store's `triggerNodeChanges`, the same internal pipeline that already calls the `onNodesChange` prop already passed to `<ReactFlow>` in `canvas.tsx` (unchanged this unit) — this is the same mechanism that already makes node dragging sync, so resizing syncs "for free" through the existing collaborative state flow, satisfying the spec's "keep all node updates connected to the existing collaborative canvas state" for resize.
- 14-node-editing: label editing uses `useReactFlow().updateNodeData(id, {label})` (called from inside `CanvasNodeRenderer`, a descendant of the `<ReactFlowProvider>` in `canvas.tsx`) rather than a new state-update path — traced its implementation the same way (`node_modules/@xyflow/react`'s source) and confirmed it also funnels through `onNodesChange` via the store's batched `setNodes`/diff mechanism, so it reaches the same already-wired Liveblocks sync as every other node mutation, with no new plumbing in `canvas.tsx`.
- 14-node-editing: a new `NodeLabelContent` sub-component (in `canvas-node.tsx`, not exported) is shared by both the CSS-shape branch (rectangle/pill/circle) and the SVG-overlay-label branch (diamond/hexagon/cylinder) so the label swaps between its static (`<span>`) and editing (`<textarea>`) states identically in both — same centered flex wrapper in both branches, only the inner content swaps, which is what keeps the swap free of layout shift (the wrapper's border/background/size never changes). The textarea uses `rows={1}` with no auto-grow, a deliberate choice: an auto-resizing textarea would itself be a layout shift, so a fixed single-row box was used instead, consistent with the spec's explicit "keep editing smooth without causing layout shifts." Placeholder text ("Add a label", `text-copy-faint`/italic) renders in the same centered position whether idle (empty `<span>` case) or actively editing (textarea's native `placeholder` attribute, styled to match via `placeholder:text-copy-faint`).
- 14-node-editing: double-clicking the label/center area (the CSS-shape branch's whole bordered `div`, or the SVG branch's absolutely-positioned label-overlay `div` — both already occupy the full node "center") sets local `isEditing` state and calls `event.stopPropagation()` so the double-click doesn't bubble to the pane's default zoom-on-double-click behavior. The SVG branch's label-overlay `div` had its pre-existing `pointer-events-none` removed (it was there so mouse hit-testing fell through to the SVG beneath it, per 13-node-shape's implementation) since it now needs to receive the double-click itself — no functional regression, since no shape currently has any hover-specific CSS that depended on hit-testing falling through to the SVG (the `ui-context.md` "revealed on node hover" connection-handle behavior is still unimplemented, tracked below).
- 14-node-editing: the textarea carries `nodrag nopan nowheel` (xyflow's built-in escape-hatch classNames, the same ones the library's own internal resize controls use) so clicking, dragging, or scrolling inside it never drags the node or pans/zooms the canvas — this is the literal mechanism satisfying "prevent text editing interactions from dragging or panning the canvas." Escape is handled by calling `event.currentTarget.blur()` (a single code path — Escape and blur both converge on the same `onBlur` handler that closes editing, rather than two separate close-editing implementations that could drift).
- 14-node-editing: verified via `tsc --noEmit` (clean), `eslint` (clean), `npm run build` (passes), and a live Playwright pass against the already-running `next dev` server, using the established `@clerk/backend` throwaway-user + synthetic-`DragEvent` drop pattern from 12-/13-shape-panel: selecting a dropped node showed exactly 4 `.react-flow__resize-control.handle` elements, deselecting removed them; dragging the bottom-right handle outward grew the node's on-screen box (320×160 → 394×214 screen px at 2x zoom); dragging the same handle far past the node's origin floored it at exactly 96×64 screen px = 48×32 flow units, i.e. precisely `NODE_MIN_SIZE`, confirming the minimum-size floor holds; **reloading the page** after the resize showed the same 96×64 dimensions, confirming the resized size is real Liveblocks-synced state, not local-only React state. Double-clicking opened a `<textarea>`; typing left the canvas's `.react-flow__viewport` `transform` style byte-for-byte unchanged (no accidental pan); a deliberate large mouse-drag gesture starting inside the textarea moved neither the viewport transform nor the node's bounding box (drag-prevention confirmed, not just assumed from the `nodrag`/`nopan` classNames being present); Escape closed the textarea and committed the typed label; **reloading the page** again showed the same label text, confirming label edits are Liveblocks-synced, not local-only; re-entering edit mode and clicking the empty canvas pane (blur, not Escape) also closed the textarea, confirming both close paths work independently. The only console errors were the already-documented Bearer-token-vs-Clerk-CDN CORS artifact (see Session Notes elsewhere in this file) — not an app defect. Test project, Clerk user/session, the temporary verification script, and the scratchpad Playwright symlink were all removed afterward.
- 14-node-editing bugfix (user-reported, same session): the first pass's editing textarea used `rows={1}` + `overflow-hidden` with no auto-growth. Typing enough text to wrap past one line clipped every wrapped line above the current one (only reappearing on blur, when the static `<span>` — which wraps with no height cap — took over) — the fixed single-row height was clipping the *editing* view specifically, not a data-loss bug. Fixed by extracting the textarea into a new `AutoSizingTextarea` (`canvas-node.tsx`), which resizes its own `style.height` to `scrollHeight` in a `useLayoutEffect` keyed on the label value, so it grows before paint on every keystroke instead of staying pinned to one row — it still sits inside the same centered flex wrapper as the static label, so it stays horizontally and vertically centered as it grows, matching the "keep the label centered" requirement from the original spec. Verified live: typed a long multi-word label, confirmed `scrollHeight === clientHeight` throughout typing (no hidden overflow), confirmed the full typed string round-tripped through `updateNodeData` with nothing dropped, and confirmed via a screenshot that all four wrapped lines stayed visible and centered while typing.

- 15-node-color-toolbar: `components/editor/node-color-toolbar.tsx` (new) — `NodeColorToolbar`, a thin wrapper around `@xyflow/react`'s built-in `<NodeToolbar>` primitive (`position={Position.Top}`, `offset={14}`) rather than hand-rolled positioning: it already resolves the current node via the same `useNodeId()` context `NodeResizer` uses (no `nodeId` prop needed), auto-hides unless its node is the single selected node, and portals into `.react-flow__renderer` so it renders above the node without participating in the node's own layout/overlap — satisfies "only show it when the node is selected" and "keep it slightly above the node without overlapping it" for free. Renders one `role="radio"` button per `NODE_COLORS` entry (8 swatches, per `ui-context.md`'s Node Color Palette). No new color data was added — this unit is UI-only on top of the palette 11-base-canvas already defined.
- 15-node-color-toolbar: swatch styling — active swatch gets a 2px border in its own paired text color plus a centered `lucide-react` `Check` icon (the "clearly selected" requirement: a border alone read as too subtle in a live check, so a checkmark was added on top). Hover glow is a tight, small-blur `box-shadow` (`0 0 6px 0px`) in the swatch's own text color, applied via a Tailwind arbitrary-value class reading a per-swatch inline CSS custom property (`--swatch-glow`) — necessary because the glow color is per-swatch data, not a fixed token, so a static Tailwind class alone couldn't express it. The toolbar container carries `nodrag nopan nowheel` (the same xyflow escape-hatch classNames 14-node-editing's label textarea already established) so clicking a swatch can never drag the node or pan/zoom the canvas underneath it.
- 15-node-color-toolbar: `components/editor/canvas-node.tsx`'s `CanvasNodeRenderer` renders `NodeColorToolbar` (via a shared `colorToolbar` JSX variable, alongside the existing `resizer` variable) in both the CSS-shape branch and the SVG-shape branch, wired to a new `handleSelectColor` callback that calls `updateNodeData(id, { color: fill })` — the exact same `useReactFlow().updateNodeData` mechanism 14-node-editing already traced through xyflow's internals as flowing into the existing Liveblocks-backed `onNodesChange` pipeline for label edits, so swatch selection syncs collaboratively with zero new plumbing in `canvas.tsx`. Text color updates automatically since `CanvasNodeRenderer` already derives its border/text color from `getNodeColorByFill(data.color)` (from 12-shape-panel) — setting `color` alone is sufficient to update both fill and text, per spec.
- 15-node-color-toolbar: verified via `tsc --noEmit` (clean), `eslint` (clean on both changed/new files), `npm run build` (passes), and a live Playwright pass against the already-running `next dev` server using the established `@clerk/backend` Bearer-token + synthetic-`DragEvent`-drop pattern from prior canvas units: dropped a node, confirmed 0 color swatches present before selection and exactly 8 after selecting it; clicked the Blue swatch and confirmed both the node's computed `background-color` (`rgb(16,35,61)` = `#10233D`) and `color` (`rgb(82,168,255)` = `#52A8FF`) updated immediately, matching the palette's Blue pair exactly; confirmed the canvas viewport's `transform` and the node's own `transform` were byte-for-byte unchanged after the swatch click (no accidental pan or node drag); reloaded the page and confirmed the new background color persisted (Liveblocks-synced, not local-only React state); confirmed the active swatch renders a checkmark; clicked an empty area of the canvas pane and confirmed the toolbar's swatches disappeared (0 visible), confirming it only shows for a selected node. A screenshot additionally confirmed the toolbar's visual position (centered above the node, not overlapping it) and the tight hover glow on an unselected swatch. No console errors beyond the already-documented Bearer-token-vs-Clerk-CDN CORS artifact (see Session Notes elsewhere in this file). Test project, Clerk user/session, and the temporary verification script were all removed afterward; the scratchpad Playwright symlink used for the run was also removed.

- 16-edge-behavior: `types/canvas.ts` extended (not replaced) with `CanvasEdgeData` (`{label: string}`, replacing the old placeholder `Record<string, never>` edge data type — the first unit to give edges real data) and three canvas-visual constants following the same "centralize in `types/canvas.ts`" precedent `NODE_COLORS`/`NODE_DEFAULT_SIZES` set: `EDGE_COLOR` (`#f8fafc`, from `ui-context.md`'s Edge Style section), `EDGE_STROKE_WIDTH` (`1.5`), and `CONNECTION_HANDLE_COLOR` (`#ffffff` — white isn't one of the app's surface/text tokens, it's a literal canvas-specific value the same way the node fill/text hex pairs are, so it's centralized here rather than inlined in a component).
- 16-edge-behavior: `components/editor/canvas-node.tsx` gained a `NodeHandles` sub-component rendering one `@xyflow/react` `Handle` per side (top/right/bottom/left, `Position` enum) in both shape branches. Read `@xyflow/system`'s compiled connection logic (`node_modules/@xyflow/system/dist/esm/index.mjs`, `node_modules/@xyflow/react/dist/esm/index.mjs`'s `HandleComponent`) to confirm a single `type="source"` handle per side is sufficient to satisfy "connect from any handle to any other handle": in `ConnectionMode.Loose` (already set on `<ReactFlow>` since 11-base-canvas), the `isPossibleEndHandle` check that normally requires opposite source/target types is skipped entirely, and any handle — regardless of its declared `type` — can both start a connection drag and accept one being dropped on it. This avoided the alternative (two stacked source+target handles per side, the pattern shown in some xyflow examples) as unnecessary complexity for this app's Loose-only connection mode. Hidden by default via `opacity-0`, faded in via `group-hover:opacity-100` — both shape branches' outer wrapper `div` gained a `group` class (previously absent on the CSS-shape branch, already present on the SVG branch) so hover state can reach the handles as children.
- 16-edge-behavior: `components/editor/canvas-edge.tsx` (new) — `CanvasEdgeRenderer`, a hand-rolled custom edge (not built on top of `<BaseEdge>`) so hover/selection brightening could use a Tailwind `peer`/`peer-hover:` pair between two sibling `<path>` elements: an invisible wide interaction path (`strokeWidth 24`, `pointerEvents: "stroke"`, class `peer`) drives hover state for the visible path (`peer-hover:opacity-100`) without touching its own stroke width — this is the literal mechanism behind "easier to hover and click without increasing the visible line thickness." Rest-state opacity is `0.6`, forced to `1` when `selected` is true (satisfies "dimmed at rest, brighten on hover or selected"). Routing uses `getSmoothStepPath` (clean right-angle routing with rounded step corners, per spec) and its returned `labelX`/`labelY` are passed straight into `EdgeLabelRenderer` with no manual midpoint math, per the spec's explicit instruction.
- 16-edge-behavior: label editing — double-clicking either visible path or the `EdgeLabelRenderer` div sets local `isEditing`/`draftLabel` state (not committed to `updateEdgeData` per keystroke, unlike 14-node-editing's node-label pattern — this unit's spec explicitly says "save the label on blur, Enter, or Escape," read as a deliberately different, more literal instruction than node editing's continuous-sync approach). Enter and Escape both converge on `event.currentTarget.blur()` (same single-code-path convergence pattern 14-node-editing established for its textarea), and `onBlur` is the one place that calls `updateEdgeData(id, {label: draftLabel.trim()})` — confirmed via `node_modules/@xyflow/react`'s `types/instance.d.ts` that `updateEdgeData` exists and mirrors `updateNodeData`'s already-traced (14-node-editing) merge-into-`onEdgesChange` pipeline, so no new Liveblocks plumbing was needed. The input's width is set inline via `ch` units derived from the draft label's character count (`Math.max(draftLabel.length, 1) + 1`) — "grows with the label text" without a hidden-span measurement trick. Saved non-empty labels render as a small `rounded-full` pill (`<span>`); a selected edge with an empty label shows a fainter, unbordered-emphasis "Add label" pill instead of nothing, satisfying "when an active edge has no label, show a faint hint" (read "active" as "selected," the only edge-specific transient state this app tracks); an unselected, unlabeled edge renders no pill at all, to avoid cluttering every edge on the canvas. The `EdgeLabelRenderer` wrapper carries `nodrag nopan nowheel` plus an explicit `pointerEvents: "all"` inline style — required per `EdgeLabelRenderer`'s own doc comment, since the portal has no pointer events by default — which is the literal mechanism behind "prevent label clicks and typing from dragging or panning the canvas."
- 16-edge-behavior: `components/editor/canvas.tsx` registers `edgeTypes={{canvasEdge: CanvasEdgeRenderer}}` (module-level constant, same unstable-object-avoidance pattern `nodeTypes` already used) and a new `defaultEdgeOptions` (`type: "canvasEdge"`, `data: {label: ""}`, `style: {stroke: EDGE_COLOR, strokeWidth: EDGE_STROKE_WIDTH}`, `markerEnd: {type: MarkerType.ArrowClosed, color: EDGE_COLOR}`). Read the compiled `EdgeWrapper` source (`node_modules/@xyflow/react/dist/esm/index.mjs`, the `edge = defaultEdgeOptions ? {...defaultEdgeOptions, ...edge} : edge` line) to confirm `defaultEdgeOptions` is applied as a fallback merge to *every* rendered edge, not just ones created via `onConnect` — so this single declarative object satisfies both "add a default style for new edges" and "make new connections use the custom canvas edge renderer" with zero changes to the existing `onConnect` handler already wired from 11-base-canvas.
- 16-edge-behavior: verified via `tsc --noEmit` (clean, after `next typegen`), `eslint` (clean on all new/changed files), `npm run build` (passes), and a live Playwright pass against the already-running `next dev` server, reusing the established `@clerk/backend` throwaway-user + `Authorization: Bearer` session-token pattern from prior units (this time via `clerk.sessions.createSession`+`getToken` directly, rather than driving the sign-in UI) plus the synthetic-`DragEvent` shape-drop technique from 12-/13-shape-panel. Confirmed: dropped two nodes; a handle's computed `opacity` was `0` before hover and `1` after hovering its node, with exactly 4 `.react-flow__handle` elements per node (top/right/bottom/left); a real mouse drag from one node's right handle to the other's left handle produced exactly one `.react-flow__edge`, rendered via the custom renderer (`.react-flow__edge-path` carried the expected `opacity-60 peer-hover:opacity-100` classes) with `marker-end` resolving to a real arrow marker colored `#f8fafc`; selecting the new (label-less) edge showed the faint "Add label" hint; double-clicking opened the edit input, whose inline `style.width` was `13ch` while a 12-character label was mid-typing (`length + 1`, confirming the grow-with-text behavior); a deliberate mouse-drag gesture starting inside the open input left the canvas's `.react-flow__viewport` `transform` byte-for-byte unchanged (drag/pan prevention confirmed, not just assumed from the `nodrag`/`nopan` classes being present); pressing Enter committed the typed label, which then rendered as a pill reading exactly the typed text; **reloading the page** showed the same pill text and the same edge count, confirming the label and the connection itself are both real Liveblocks-synced state, not local-only React state. A screenshot additionally confirmed the visual result: clean right-angle (step) routing, a solid triangular arrowhead at the target end, and a legible pill badge sitting on the path's midpoint. Test project, Clerk user/session, the temporary verification script, and the scratchpad Playwright symlink were all removed afterward — confirmed zero leftover `E2E Edge Behavior`-named projects and zero leftover `e2e-16-*` Clerk users post-cleanup (two were found and removed from earlier failed script iterations during this same session, before the script's own try/finally cleanup was added).

- 17-canvas-ergonomics: `components/editor/canvas-control-bar.tsx` (new) — `CanvasControlBar`, a pill-shaped floating control bar at `bottom-6 left-6` (bottom-left, per spec), styled to match `ShapeToolbar`'s existing pill convention (`rounded-full`, `bg-elevated/95` + `backdrop-blur-sm`, `border-surface-border`). Two button groups (zoom out/fit view/zoom in, then undo/redo) separated by a `w-px` divider. `z-30`, one step above `ShapeToolbar`'s `z-20`, satisfying the spec's "sit above the shape panel" as a stacking-order guarantee — the two toolbars occupy different corners (bottom-left vs. bottom-center) so they don't visually overlap in normal use.
- 17-canvas-ergonomics: zoom buttons call `useReactFlow()`'s `zoomIn`/`zoomOut`/`fitView` directly with `{ duration: 250 }` (a short, deliberate ease rather than an instant jump — "use a short animation so the movement feels smooth"). Undo/redo buttons call `useUndo()`/`useRedo()` from `@liveblocks/react/suspense` (the same suspense entry point every other Liveblocks import in this app already uses, e.g. `canvas-room.tsx`'s `useErrorListener`) and disable via `useCanUndo()`/`useCanRedo()`; disabled styling is `disabled:opacity-40 disabled:pointer-events-none` on a shared `ControlButton` sub-component. No new Liveblocks plumbing was needed — `useUndo`/`useRedo` operate on the room's shared history directly, the same history every existing node/edge mutation already writes into.
- 17-canvas-ergonomics: `hooks/use-keyboard-shortcuts.ts` (new) — `useKeyboardShortcuts({ reactFlowInstance, undo, redo })`, a `window`-level `keydown` listener (added/removed in a `useEffect`). Bails out immediately via `isEditableTarget()` (checks `tagName === "INPUT" | "TEXTAREA"` or `isContentEditable`) before evaluating any shortcut, satisfying "ignore shortcuts while typing in inputs, textareas, or editable text fields" — this is what keeps the shortcuts from firing while a node label (`canvas-node.tsx`'s `AutoSizingTextarea`, from 14-node-editing) or an edge label (`canvas-edge.tsx`'s inline input, from 16-edge-behavior) is being edited, with no changes needed to either of those components. Bindings: `+`/`=` → `zoomIn`, `-` → `zoomOut`, `Cmd/Ctrl+Z` → undo, `Cmd/Ctrl+Shift+Z` → redo, `Cmd/Ctrl+Y` → redo — all via `event.preventDefault()` so they don't also trigger the browser's own native zoom/undo. Called once from `canvas.tsx`'s `FlowCanvas`, passed the full `useReactFlow()` instance (not just the two zoom methods) since that's the literal "receive the React Flow instance" the spec asked for.
- 17-canvas-ergonomics: `components/editor/canvas.tsx` — removed the `MiniMap` import and its `<MiniMap />` render (per spec's "remove the minimap at the bottom right"); no other render-tree changes. `FlowCanvas` now also renders `<CanvasControlBar />` as a sibling of `<ShapeToolbar />`, and calls the new `useUndo`/`useRedo`/`useKeyboardShortcuts` hooks — the same `undo`/`redo` callbacks are passed to both `CanvasControlBar` (indirectly, since it calls its own `useUndo`/`useRedo`) and the keyboard hook, so both entry points to the same action stay in sync with Liveblocks' history state with no shared local state needed.
- 17-canvas-ergonomics: verified via `tsc --noEmit` (clean, after `next typegen`), `eslint` (clean on all new/changed files), `npm run build` (passes), and a live Playwright pass against `next dev` using the established `@clerk/backend` + Bearer-token + synthetic-`DragEvent`-drop pattern from prior canvas units (reused a prior session's scratchpad Playwright/Chromium install, symlinked in and removed after use, same as before). Confirmed: `.react-flow__minimap` count is `0`; undo/redo buttons start disabled and flip to enabled the instant a node is dropped; clicking Undo removes the dropped node, clicking Redo restores it; zoom-out-then-zoom-in via the buttons visibly changes the viewport's `transform: scale(...)` (zoom-in alone showed no change on the first pass only because `fitView` on a single small node already sits at the default `maxZoom` of `2` — not a bug, confirmed by reading the raw `transform` value); the `-` key changes the viewport the same way; `Ctrl+Z` removes the node, `Ctrl+Shift+Z` and `Ctrl+Y` both restore it; with a node's label textarea focused, both `-` and `Ctrl+Z` left the viewport's `transform` and the node count completely unchanged, confirming the editable-field guard works. One test-harness-only finding, not an app defect: Next.js 16 dev mode's own dev-tools indicator badge renders at `bottom-left`, the same corner as the new control bar, and visually overlaps its zoom buttons in dev — simulated mouse clicks on those buttons were intercepted by the badge until the verification switched to firing `.click()` directly via `page.evaluate` (bypasses hit-testing); this doesn't exist in production builds and doesn't affect real users, but is worth knowing for any future dev-mode Playwright pass that clicks something in that corner. No console errors beyond the already-documented Bearer-token-vs-Clerk-CDN CORS artifact. Test project, Clerk user/session, the temporary verification script, and the scratchpad Playwright symlink were all removed afterward.

- 18-starter-template: `components/editor/starter-templates.ts` (new) — `CanvasTemplate` (`{id, name, description, nodes: CanvasNode[], edges: CanvasEdge[]}`) and `CANVAS_TEMPLATES` (3 entries: Microservices Architecture, CI/CD Pipeline, Event-Driven System). Built entirely from the shared `types/canvas.ts` schema and the existing `NODE_COLORS` palette (destructured by name — `BLUE`/`PURPLE`/`ORANGE`/`RED`/`GREEN`/`TEAL` — so the template data reads by color intent instead of palette index) and `NODE_DEFAULT_SIZES` (from 12-shape-panel). Two small helpers per spec's "add small helper functions if needed": `templateNode(id, shape, position, label, color)` (fills in `type: "canvasNode"` and the shape's default width/height) and `templateEdge(id, source, target, label?)` (fills in `type: "canvasEdge"`) — every template is declared as plain calls to these two, not hand-built node/edge object literals.
- 18-starter-template: `components/editor/starter-templates-modal.tsx` (new) — `StarterTemplatesModal`, a shadcn `Dialog` (`sm:max-w-2xl` override on the default `sm:max-w-sm`) with a `ScrollArea`-wrapped grid of template cards (2-column at `sm:`, matching `share-dialog.tsx`'s existing Dialog usage pattern). Each card renders a `TemplatePreview`, the template's name/description, and an `Import` button; clicking it calls the `onImport` prop with that template then closes the dialog (`onOpenChange(false)`), per spec.
- 18-starter-template: `TemplatePreview` (in the same file) draws the diagram with a single inline `<svg>` per card — no React Flow instance, per spec's explicit "keep the preview lightweight." `getTemplateBounds()` computes a tight bounding box from the template's node positions/sizes (with a fixed flow-unit padding) and that becomes the SVG's `viewBox`, so the whole diagram auto-fits the card's fixed-size (`h-36`) viewport regardless of the template's actual footprint. Edges are drawn as plain `<line>`s between `nodeCenter()`s (`vector-effect="non-scaling-stroke"` so they stay a crisp 1.5px regardless of viewBox-to-viewport scale). Nodes reuse the exact same shared shape geometry constants (`DIAMOND_POINTS`, `HEXAGON_POINTS`, `CYLINDER_BODY_PATH`, `CYLINDER_TOP_ELLIPSE`, from 13-node-shape) that `CanvasNodeRenderer` and `ShapeToolbar`'s drag preview already draw from, transformed into each node's actual bounding box via `<g transform="translate(...) scale(...)">` — so a template preview can never visually drift from what the shape looks like once it's a real node on the canvas.
- 18-starter-template: `components/editor/canvas.tsx`'s `FlowCanvas` gained `handleImportTemplate`, wired as `StarterTemplatesModal`'s `onImport`. First pass (superseded the same session, see the follow-up note below) cleared the canvas before adding the template. **Real finding from that first pass's live verification, still true and still relevant**: clearing cannot go through `onNodesChange`/`onEdgesChange` with a `"remove"` change — reading `node_modules/@liveblocks/react-flow/dist/lib/flow.js` showed `applyNodeChanges`/`applyEdgeChanges` both implement `case "remove": break` as a **deliberate no-op**; the library's only real deletion path is the separate `onDelete({nodes, edges})` mutation (already wired to `<ReactFlow onDelete={onDelete}>` since 11-base-canvas), which calls `nodesMap.delete(id)`/`edgesMap.delete(id)` directly. This is now moot for the import flow itself (which no longer deletes anything, see below) but stays true for any future canvas-clearing feature.
- 18-starter-template follow-up (same-day, user-reported): the spec's "clear all existing nodes and edges first" behavior turned out to be unwanted in practice — importing a template while a user already had shapes placed silently destroyed their work. Per explicit user instruction, `handleImportTemplate` was changed to **never delete anything**: it now computes the current canvas's bounding box (`getBoundingBox(nodes)`, from node `position`/`width`/`height`) and offsets the template's own bounding box to sit `IMPORT_GAP` (120 flow units) to the right of it, top-aligned — so a template always lands to the side of existing content, never on top of or replacing it. This directly contradicts this feature's original spec bullet ("make sure the starter template replaces the current canvas instead of being added on top of existing work") — recorded here as a deliberate, user-directed deviation, not an oversight. Also fixed as part of this change: template node/edge ids (e.g. `"client"`) are only unique within one template's own definition, so importing the same template twice (now possible, since nothing is cleared between imports) needed remapping every id to a per-import-unique prefix (`` `${template.id}-${Date.now()}-${originalId}` ``) — without it, a second import of the same template would reconcile into the first copy's nodes instead of creating a second one, per the same `applyNodeChanges` "add" behavior noted above (`existing ? existing.reconcile(...) : nodes.set(...)`).
- 18-starter-template: the "add a navbar button" + "wire into the editor" steps required threading modal open/close state from `WorkspaceNavbar` (top of the workspace, sibling of the canvas) down to `FlowCanvas` (bottom, inside `ReactFlowProvider`/`RoomProvider`/Suspense, the only place `onNodesChange`/`onEdgesChange`/`onDelete`/`fitView` actually exist) — done as plain prop drilling (`isTemplatesModalOpen`/`onTemplatesModalOpenChange`) through `WorkspaceShell` (owns the state) → `CanvasArea` → `CanvasRoom` → `Canvas` → `FlowCanvas`, not a new Context. Unlike 07-wire-editor-home's `ProjectActionsContext` (needed because `EditorHome` is rendered as opaque `{children}` from a server component, with no direct JSX prop channel), every component on this path is a plain client component already passing props explicitly (`roomId` already threads the same way) — a Context would have been an unjustified extra abstraction for a 4-hop boolean+callback chain the codebase already threads props through elsewhere. `WorkspaceNavbar` gained an `onOpenTemplates` prop and a `LayoutTemplate`-icon "Templates" button (styled to match the existing `Share` button), placed to its left.
- 18-starter-template: verified via `tsc --noEmit` (clean), `eslint` (clean on all new/changed files), `npm run build` (passes), and two live Playwright passes against the already-running `next dev` server (reused a prior session's scratchpad Playwright/Chromium install, symlinked into `node_modules/playwright` and removed after use each time) using the established `@clerk/backend` throwaway-user + Bearer-token pattern, plus the synthetic-`DragEvent` shape-drop technique from 12-/13-shape-panel. First pass (clear-then-import behavior, now superseded): modal opened with exactly 3 template cards each with a correctly-shaped/colored SVG preview (screenshot-verified); import produced exactly the template's 7 nodes/6 edges (a manually-dropped node was gone, not left over — this is what caught the `onDelete`-vs-`onNodesChange` bug above); reload showed the same counts, confirming real Liveblocks sync. Second pass (current side-by-side behavior): dropped a manual node, confirmed it survived a template import untouched (`Node ids after 1st import` included both the manual node and 7 new `microservices-<timestamp>-*` ids); imported the *same* template a second time and confirmed all 15 node ids were unique and 12 edges existed (no id collisions between the two copies); confirmed via each node's `.react-flow__node`'s `transform` that the two template copies and the manual node sat side by side with no overlapping x-ranges; screenshot confirmed the visual layout (manual node leftmost, two full microservices diagrams to its right); reload showed the same 15-node count, confirming Liveblocks sync. No console errors in either pass. Test projects, Clerk users/sessions, temporary verification scripts, and the scratchpad Playwright symlink were all removed afterward.

- 19-presence-avatars-cursor: `liveblocks.config.ts`'s `Presence.isThinking` renamed to `Presence.thinking` (only two call sites — the type declaration itself and `RoomProvider`'s `initialPresence` in `components/editor/canvas-room.tsx`) to match this unit's spec literally, which names the field `thinking`. No other behavior change — `thinking` is still unused by any UI, same as `isThinking` was before this unit.
- 19-presence-avatars-cursor: `components/editor/presence-avatars.tsx` (new) — `PresenceAvatars`, a floating pill (`absolute top-4 right-4 z-30`, same `bg-elevated/95`/`backdrop-blur-sm`/`border-surface-border-subtle` convention as `ShapeToolbar`/`CanvasControlBar`) rendered inside the canvas area, not the workspace navbar — a second, canvas-local presence surface distinct from `WorkspaceNavbar`'s own pre-existing `UserButton`, per the spec's explicit "keep it visually separate from the main navbar actions" + "position it in the top-right corner of the editor canvas view." Reads `useOthers()` (`@liveblocks/react/suspense`) for collaborator presence and Clerk's `useUser()` for the current user's ID, filtering `other.id !== user.id` — this filter is not redundant with `useOthers()` already excluding the caller's own connection: a user with two tabs open produces two distinct Liveblocks connections sharing one Clerk `id`, and the spec's literal instruction is to exclude by Clerk user ID specifically (so a second tab of your own session never shows up as a "collaborator" or gets its own cursor). Shows up to 5 collaborator avatars in an overlapping stack (`-space-x-2`), a `+N` overflow chip beyond that, a divider (`only when at least one collaborator exists`), then a `<UserButton appearance={{elements:{userButtonAvatarBox: "h-7 w-7"}}}>` sized to match the `h-7 w-7` collaborator avatars exactly. Collaborator avatars use `other.info.avatar`/`other.info.name`/`other.info.color` (Clerk-sourced `UserMeta`, already set server-side in `app/api/liveblocks-auth/route.ts` since 10-liveblocks-setup) — image when present, else an initials circle filled with the participant's presence color; each avatar carries `ring-2 ring-elevated` per spec's "subtle ring so avatars stay readable on the dark canvas."
- 19-presence-avatars-cursor: `components/editor/presence-cursors.tsx` (new) — `PresenceCursors`, a `pointer-events-none fixed inset-0 z-40` overlay rendering one colored pointer SVG + name badge per other participant with a non-null `presence.cursor`, filtered the same `other.id !== user.id` way as `PresenceAvatars` (never renders the caller's own cursor, including from a second tab). Cursor positions are stored in flow coordinates (matching `Presence.cursor`'s existing `{x,y}|null` shape) and converted to viewport pixels via `useReactFlow().flowToScreenPosition()` at render time — confirmed via `node_modules/@xyflow/react`'s compiled source that this function returns real client/viewport coordinates (adds the React Flow DOM node's `getBoundingClientRect()` offset), which is why a plain `position: fixed` overlay (not scoped to the canvas wrapper div) renders cursors in the correct place regardless of navbar height, sidebar width, or AI-panel presence. Also calls `useViewport()` purely to force a re-render on pan/zoom — `flowToScreenPosition` itself always reads live viewport state, but nothing else in this component would otherwise trigger React to recompute cursor positions while the local user pans/zooms without any presence change.
- 19-presence-avatars-cursor: `components/editor/canvas.tsx`'s `FlowCanvas` gained `handlePaneMouseMove`/`handlePaneMouseLeave`, wired to `<ReactFlow onPaneMouseMove={...} onPaneMouseLeave={...}>` — confirmed via `@xyflow/react`'s type defs that `onPaneMouseMove`/`onPaneMouseLeave` are the only React-Flow-level mouse-move/leave props that exist (there is no bare `onMouseMove` prop on `<ReactFlow>` itself), i.e. these are what the spec's "update cursor position on React Flow's onMouseMove event" / "clear cursor to null on mouse leave" literally map to. `handlePaneMouseMove` calls `useUpdateMyPresence()` (`@liveblocks/react/suspense`) with `{cursor: screenToFlowPosition({x: event.clientX, y: event.clientY})}` — broadcasting in flow coordinates (not raw screen coordinates) is what makes `PresenceCursors`' `flowToScreenPosition` round-trip correct for every other viewer regardless of their own pan/zoom state. `PresenceAvatars`/`PresenceCursors` render as new siblings of `<ReactFlow>`/`ShapeToolbar`/`CanvasControlBar` inside `FlowCanvas`'s existing wrapper div.
- 19-presence-avatars-cursor: deliberately hand-rolled cursor broadcast/render instead of using `@liveblocks/react-flow`'s built-in `<Cursors />` component, even though its default `presenceKey` ("cursor") happens to match this app's own `Presence.cursor` field. Read `node_modules/@liveblocks/react-flow/dist/cursors.js` to confirm `<Cursors />` broadcasts via raw `pointermove`/`pointerleave` DOM listeners (not React Flow's `onPaneMouseMove`/`onPaneMouseLeave` props) and renders via `@liveblocks/react-ui`'s `Cursor` component — this app has never imported `@liveblocks/react-ui` anywhere else, and the spec's literal wording ("React Flow's onMouseMove event") plus the codebase's established precedent of hand-rolling canvas UI on top of shared tokens (`canvas-edge.tsx` not using `<BaseEdge>`, `shape-toolbar.tsx`'s drag preview not using a second React root) both pointed at a custom implementation over the pre-built one.
- 19-presence-avatars-cursor: verified via `tsc --noEmit` (clean), `eslint` (clean on all new/changed files), `npm run build` (passes), and a live Playwright pass against `next dev` using the established `@clerk/backend` two-throwaway-user + Bearer-token pattern (owner + a seeded `ProjectCollaborator`), both loading the same room concurrently. Confirmed via direct DOM inspection: before the collaborator joined, the presence pill had 0 collaborator avatars and no divider (`hasDivider: false`); after the collaborator joined, exactly 1 collaborator avatar appeared (`title="Collab Test"`, `<img>` sourced from a real `img.clerk.com` URL) with the divider now present; moving the collaborator's mouse over `.react-flow__pane` made a cursor with a "Collab Test" badge appear on the owner's screen; the owner moving their own mouse did not add a second (self) cursor; dispatching `mouseleave` on the collaborator's pane dropped the owner-visible cursor overlay back to 0 children, confirming the clear-on-mouse-leave path. A screenshot additionally confirmed the visual result: an orange pointer + rounded name badge tracking near the canvas center, colored via the collaborator's server-assigned `getCursorColor()` value. **One inconclusive sub-check, not treated as a defect**: `UserButton`'s own avatar image never finished mounting inside either the new `PresenceAvatars` pill or the pre-existing `WorkspaceNavbar` instance during this same test run (`data-clerk-component="UserButton"` mounted but stayed empty, with the page eventually throwing `ClerkRuntimeError: failed_to_load_clerk_js`) — traced this to the same already-documented Bearer-token-vs-Clerk-CDN artifact noted in this file's Session Notes (03-auth, 08-editor-workspace-shell eras): the synthetic `Authorization` header sent context-wide also reaches Clerk's `*.clerk.accounts.dev` CDN and trips a CORS failure loading `clerk-js`, independent of anything this unit changed — confirmed identical failure on the untouched pre-existing navbar `UserButton` in the same run, so this is a test-harness limitation, not a regression. Test project, Clerk users, and temporary verification scripts were all removed afterward.

- 20-ai-sidebar-shell: `components/editor/ai-sidebar.tsx` (new) — `AiSidebar`, extracted out of `workspace-shell.tsx`'s old inline `AiSidebarPlaceholder` (a non-floating `w-80 shrink-0` flex sibling with no animation) and rebuilt as a floating slide-in panel matching `ProjectSidebar`'s established pattern, mirrored to the right (`fixed top-[4.25rem] right-3 bottom-3 z-40`, `translate-x-[calc(100%+0.75rem)]` when closed vs. `translate-x-0` when open, `bg-elevated/95`/`backdrop-blur-sm`/`border-surface-border`/`shadow-2xl`, `pointer-events-none`+`aria-hidden` when closed). Open/close state is still owned entirely by `WorkspaceShell` (`isAiSidebarOpen`, unchanged) — only the rendered component and its position in the tree changed, per the spec's "don't rebuild the existing sidebar open/close behavior."
- 20-ai-sidebar-shell: header has a small inline `Bot` icon, "AI Workspace" title (`text-copy-primary`), "Collaborate with Ghost AI" subtitle (`text-copy-muted`), and a close button — same structural convention as `ProjectSidebar`'s header. Below it, a shadcn `Tabs` with "AI Architect"/"Specs": the spec's literal `bg-accent`/`text-accent`/`text-accent-text` class names don't exist in this app's token set (`bg-accent`/`text-accent` resolve to shadcn's generic light-grey primitive `--accent`, and `text-accent-text` isn't a defined token at all) — read as informal spec language for "this sidebar's accent color," mapped instead to the app's actual AI-accent tokens (`bg-ai`, `text-ai-text`, already used by the pre-existing placeholder this unit replaced) throughout: active tab background/text, starter-chip text, assistant-message text, and both `text-white`-on-accent buttons (Send, Generate Spec).
- 20-ai-sidebar-shell: AI Architect tab — empty state (`Bot` icon at `text-copy-faint`, matching `ProjectSidebar`'s `EmptyProjectsState` icon convention, short description, 3 starter-prompt pill chips styled `bg-subtle text-ai-text`) that switches to a scrollable message list once at least one message exists. Since this unit is UI-structure-only (no backend, no AI generation, per scope limits), the chat area is backed by local component state only: clicking a starter chip fills the textarea (doesn't auto-send), and submitting appends a user-role bubble (right-aligned, `bg-accent-dim border-2 border-brand/50 text-copy-primary`) with no assistant reply — this demonstrates the required message-bubble styling (both user and assistant variants are implemented and available to any future message) without simulating real AI behavior. The `Textarea` relies on the existing shadcn component's built-in `field-sizing-content` CSS (no JS auto-resize logic needed) with `min-h-[72px] max-h-40` overrides for the spec's exact bounds; `Enter` submits, `Shift+Enter` inserts a newline (`onKeyDown` checks `event.shiftKey`).
- 20-ai-sidebar-shell: Specs tab — static `Generate Spec` button (`bg-ai text-white`, no click behavior yet) above one demo spec card (`bg-elevated`/`border-surface-border`, `FileText` icon, title, 3-line-clamped description, a `disabled` `Download` button) — exactly the "static demo spec card" the spec asked for for now, no generation/persistence logic.
- 20-ai-sidebar-shell: verified via `tsc --noEmit` (clean), `eslint` (clean on both changed/new files), `npm run build` (passes), and a live Playwright pass against `next dev` (reused a prior session's scratchpad Playwright/Chromium install, symlinked into `node_modules/playwright` and removed after use, same pattern as prior units) using the established `@clerk/backend` throwaway-user + Bearer-token pattern. Confirmed: sidebar opens/closes via the navbar's existing AI toggle with the slide transition (`aria-hidden` flips accordingly); AI Architect tab renders its empty state with all 3 starter chips; clicking a chip fills the textarea with that chip's exact text; pressing Enter sends it, rendering a right-aligned user bubble with the typed text and clearing the input; switching to the Specs tab shows the Generate Spec button and the demo card with a disabled Download button. Screenshots confirmed the visual result at each step (floating panel over the canvas, tab accent styling, message bubble). Test project and Clerk user/session were deleted afterward (Prisma `deleteMany` by name for the project row, since `ownerId` has no DB-level FK to Clerk users and doesn't cascade automatically); the scratchpad Playwright symlink and temporary verification/cleanup scripts were also removed.

- 21-canvas-autosave: installed `@vercel/blob` (`^2.8.0`) — the only new dependency this unit needed. `prisma/models/project.prisma`'s existing `canvasJsonPath` field (added in 05-prisma but never populated by any code path until now) is reused as-is for the saved-canvas blob URL, per the spec's "add or reuse a field" — no migration was needed.
- 21-canvas-autosave: `app/api/projects/[projectId]/canvas/route.ts` (new) — `PUT` (validates `{nodes: unknown[], edges: unknown[]}` from the request body, uploads `JSON.stringify({nodes, edges})` to Vercel Blob at a stable per-project pathname `canvases/{projectId}.json` via `put(..., {access: "private", addRandomSuffix: false, allowOverwrite: true})`, then writes the returned `blob.url` to `Project.canvasJsonPath`) and `GET` (reads `canvasJsonPath` from Prisma, returns `{canvas: null}` if unset, otherwise fetches the blob via `@vercel/blob`'s `get()` and returns its parsed JSON as `{canvas}`). Both use `checkProjectAccess` (owner-or-collaborator, the same helper `09-share-dialog`'s collaborator-list `GET` uses) rather than `findOwnedProject` — canvas save/load has to work for every participant in a collaborative room, not just the owner.
- 21-canvas-autosave bugfix (found during live verification, not in the original spec): the first pass used `access: "public"` on `put()`, which failed at runtime with `Vercel Blob: Cannot use public access on a private store` — this Vercel Blob store is configured private (an environment fact, not something this unit's spec anticipated). Fixed by using `access: "private"` on both `put()` and the paired `get()` read (raw `fetch(project.canvasJsonPath)` also 403'd — `Forbidden` — against a private blob's URL; `@vercel/blob`'s `get()` helper authenticates the read using `BLOB_READ_WRITE_TOKEN` the same way `put()` does). Any future unit reading/writing this project's Vercel Blob store must use `access: "private"`, not `"public"`.
- 21-canvas-autosave: `hooks/use-canvas-autosave.ts` (new) — `useCanvasAutosave(roomId, nodes, edges)`, returns a `CanvasSaveStatus` (`"idle" | "saving" | "saved" | "error"`, added to `types/canvas.ts`). Debounces via a `setTimeout`/`clearTimeout` pair keyed on `[roomId, nodes, edges]` (1500ms), skips the mount render via an `isFirstRender` ref (so opening a room with nothing changed yet never fires a save before a possible saved-canvas load has had a chance to land), and `PUT`s `{nodes, edges}` to the new canvas route on fire, setting `"saving"` immediately and `"saved"`/`"error"` from the response.
- 21-canvas-autosave: `components/editor/canvas.tsx`'s `FlowCanvas` gained a load-on-mount effect (guarded by a `hasAttemptedLoadRef`, so it runs its check exactly once per room mount) that only fetches+applies the saved canvas (via `onNodesChange`/`onEdgesChange` `"add"` changes, the same mechanism `12-shape-panel`'s node-drop and `18-starter-template`'s template-import already use) when `nodes.length === 0 && edges.length === 0` at that moment — satisfying the spec's literal "if the room already has nodes or edges, skip the load entirely." Also calls the new `useCanvasAutosave` hook and forwards its returned status to a new `onSaveStatusChange` prop.
- 21-canvas-autosave: `roomId` and `onSaveStatusChange` are threaded as plain props down the same 4-hop chain `18-starter-template` already established for `isTemplatesModalOpen` (`WorkspaceShell` → `CanvasArea` → `CanvasRoom` → `Canvas` → `FlowCanvas`), just in the opposite data-flow direction (status flows up via the callback instead of a boolean flowing down) — consistent with that unit's precedent that a plain prop chain, not a new Context, is the right tool for state shared only between plain client components already threading other props the same way. `WorkspaceShell` owns the `saveStatus` state (`useState<CanvasSaveStatus>("idle")`) and passes it straight to `WorkspaceNavbar`.
- 21-canvas-autosave: `components/editor/workspace-navbar.tsx` gained a `SaveStatusIndicator` (new, same file) — a small pill (matching the existing Templates/Share buttons' `rounded-full border-surface-border-subtle` convention) rendered first in the navbar's right-side button group. Renders nothing for `"idle"` (nothing to show before any change has happened); `"saving"` shows a spinning `Loader2` + "Saving…" in `text-copy-muted`; `"saved"` shows a `Check` + "Saved" in `text-success`; `"error"` shows a `CircleAlert` + "Save failed" in `text-error` (both tokens already existed in `globals.css`, added during `02-editor`'s Ghost-token wiring pass but unused by any component until now).
- 21-canvas-autosave: verified via `tsc --noEmit` (clean, after `next typegen`), `eslint .` (clean on all new/changed files), `npm run build` (passes, `/api/projects/[projectId]/canvas` registers), and a live Playwright pass against `next dev` using the established `@clerk/backend` throwaway-user + Bearer-token pattern plus the synthetic-`DragEvent` shape-drop technique from `12-shape-panel`. Confirmed: dropping a node showed the navbar flip through "Saving…" then "Saved" (read directly from the DOM, not just inferred from network calls); `Project.canvasJsonPath` was populated in Postgres after the debounced save fired; `GET /api/projects/{id}/canvas` returned the exact saved node; reloading a fresh room loaded the 1 saved node from the blob; dropping a 2nd node, letting it autosave, and reloading again showed 2 nodes. **A stronger, isolated check was added specifically to distinguish this unit's skip-load logic from Liveblocks Storage's own pre-existing cross-reload persistence** (which by itself would make a "did it skip the blob load" test inconclusive, since Storage already keeps nodes across reloads with no help from this unit): `PUT` a decoy 5-node payload directly to the canvas route (bypassing the UI, so the live room's Liveblocks Storage still only has the real 2 nodes), reload, and confirm the room renders 2 nodes, not 5 — proving a non-empty room's live state is never clobbered by a stale/foreign saved blob. Test project, Clerk user/session, the temporary verification script, and the scratchpad Playwright symlink were all removed afterward; confirmed zero leftover `E2E Autosave`-named projects post-cleanup.

- current-issues.md fix #1 (drag-and-drop position offset): `components/editor/canvas.tsx`'s `handleDrop` set a dropped node's `position` directly to `screenToFlowPosition({x: event.clientX, y: event.clientY})` — but a node's `position` is its **top-left corner**, not its center, so the node's corner (not its center) landed under the cursor, making the whole node appear shifted down-and-right of the drop point. `screenToFlowPosition` already correctly handles the canvas container's bounding rect and the current pan/zoom (confirmed by reading `@xyflow/react`'s compiled source, same as `19-presence-avatars-cursor`'s `flowToScreenPosition` trace) — the only missing piece was centering. Fixed by shifting the computed flow position up-and-left by half the dropped shape's own `width`/`height` (both already in flow units, so no zoom-scaling needed): `position = {x: cursorFlowPosition.x - payload.width/2, y: cursorFlowPosition.y - payload.height/2}`. This also matches `ShapeToolbar`'s existing drag-preview behavior (`13-node-shape`'s `setDragImage(preview, width/2, height/2)`, which already centers the ghost preview on the cursor regardless of where within the source button it was grabbed) — the drop now places the real node exactly where the preview was shown.
- current-issues.md fix #1 verification note: a live Playwright check (synthetic `DragEvent`s, the established technique) initially showed a large residual offset even after the fix, which traced to a *different*, unrelated behavior: `<ReactFlow fitView>` performs a one-time deferred initial fit the moment a room's nodes go from empty to non-empty — dropping the very first shape onto a truly empty canvas gets re-centered/re-zoomed by that one-time fit immediately after placement, which has nothing to do with the drop math. Re-verified by dropping a throwaway seed node first (absorbing the one-time fit), then measuring a second real drop against a stable viewport: node center landed at the exact drop pixel, 0px delta. Not an app bug — a `12-`/`17-`-era-style test-harness gotcha worth remembering for any future canvas-position verification on an empty room.
- current-issues.md fix #2 (collaborator avatar image error): `next.config.ts` gained `images.remotePatterns` allowing `https://img.clerk.com` — this is where Clerk hosts user avatar images (used by `components/editor/presence-avatars.tsx` and `components/editor/share-dialog.tsx`, both from `09-share-dialog`/`19-presence-avatars-cursor`). Both of those currently render avatars via plain `<img>` tags, not `next/image` — `remotePatterns` only gates `next/image`, so this config alone doesn't change either component's current runtime behavior; it unblocks any future move to `next/image` (or Clerk-internal component that uses it) without a second silent hostname-not-configured failure. No `<img>` → `next/image` conversion was made — out of the issue's literal scope ("Add img.clerk.com to the allowed image hostnames ... using remotePatterns").
- current-issues.md: verified via `tsc --noEmit` (clean, after `next typegen`), `eslint .` (clean), and `npm run build` (passes) per the issue's explicit scope checklist. Both fixes stayed within the listed scope — no canvas node/edge rendering changes, no editor-home navbar changes, and neither touched `hooks/use-canvas-autosave.ts`, `presence-avatars.tsx`'s/`presence-cursors.tsx`'s presence logic, or any Liveblocks room-connection code, so `21-canvas-autosave`'s and `19-presence-avatars-cursor`'s prior live verifications still hold.
- AI sidebar focus-management fix (code-review finding, re-verified against current code before fixing): `components/editor/ai-sidebar.tsx`'s `<aside>` had `aria-hidden={!isOpen}` while closed but no `inert` — its interactive descendants (textarea, starter chips, tab triggers, the close button) stayed keyboard-focusable via Tab even though the panel was invisible (`pointer-events-none`, translated off-screen), and `aria-hidden="true"` on an element with focusable descendants is itself an ARIA violation. Fixed by adding `inert={!isOpen}` alongside the existing `aria-hidden`. Also added focus restoration: closing the panel via its own X button previously left focus nowhere (the browser force-blurs a focused element the instant it becomes `inert`, and focus falls back to `<body>`), so a new `triggerRef` (a `RefObject<HTMLButtonElement | null>`, owned by `WorkspaceShell`, attached to the navbar's AI toggle button in `WorkspaceNavbar`, passed to `AiSidebar`) is focused in a `useEffect` that fires only on an open→closed transition (guarded by an `isFirstRender` ref, the same mount-skip pattern `hooks/use-canvas-autosave.ts` already uses, so the sidebar doesn't steal focus on initial page load). Opening was left untouched — no effect runs on that transition, preserving whatever focus was already in place (typically the just-clicked trigger button), matching the finding's "preserving normal focus behavior when opening."
- AI sidebar focus-management fix, verification: a live Playwright pass (fresh throwaway Clerk user, same established pattern) confirmed: the panel's `<aside>` has `inert === true`/`aria-hidden="true"` while closed and `false`/`"false"` while open; a direct `.focus()` call on the in-panel close button while closed does not move `document.activeElement` (inert genuinely blocks it, not just visually); focusing and clicking the in-panel close button moves focus to the navbar's "Open AI sidebar" button afterward, not `<body>`. One test-script snag worth remembering for future sessions: `ProjectSidebar` also renders an `<aside>`, so `document.querySelector("aside")` grabs the wrong panel — had to scope by content (`hasText: "AI Workspace"`) — and the navbar's AI toggle button's own `aria-label` flips to `"Close AI sidebar"` while open (same text as the in-panel X button), so an unscoped `button[aria-label="Close AI sidebar"]` locator matches both and needs to be scoped inside the AI `<aside>` specifically. `tsc --noEmit`, `eslint .`, and `npm run build` all clean.

- Trigger.dev infra setup (started in-session, resolved by 22-design-agent-api below): most of the scaffold was already in place (`@trigger.dev/sdk`/`@trigger.dev/build` ^4.5.10 installed, `trigger.config.ts` pointing at `proj_sggqarazemsknymoxrfv` / "ghost ai project", `tsconfig.json` include, `.gitignore`, `.mcp.json` MCP server config) but only had a placeholder `src/trigger/` (`.gitkeep`, no task) and no `TRIGGER_SECRET_KEY` in any env file. CLI is authenticated (confirmed via `whoami`, account edenradu26@gmail.com) and the project ref is confirmed valid. Added a `trigger:dev` npm script (`npx trigger.dev@4.5.10 dev`). A real `TRIGGER_SECRET_KEY` (`tr_dev_...`) is now present in `.env.local` (added between sessions, not by this unit) — 22-design-agent-api's live verification below is the first session to actually run `trigger dev` and confirm task registration works end to end.
- Follow-up: moved the task directory from `src/trigger/` to a root-level `trigger/` per the user's explicit request — this matches every other top-level directory in the project (`app`, `components`, `lib`, `hooks`, `types` are all root-level, not under `src/`) and `architecture-context.md`'s own `trigger` directory listing. `trigger.config.ts`'s `dirs` updated to `["./trigger"]`; the now-empty `src/` was removed. Deleted the placeholder `hello-world` example task and replaced it with two real stubs matching `architecture-context.md`'s documented contracts: `trigger/generate-design.ts` (`generateDesign`, id `generate-design` — payload `{projectId, prompt, canvas: {nodes, edges}}` using the existing `CanvasNode`/`CanvasEdge` types from `types/canvas.ts`, fetches the project via `lib/prisma.ts` for context) and `trigger/generate-spec.ts` (`generateSpec`, id `generate-spec` — payload `{projectId, canvas}`, output `{markdown}`). Both throw `AbortTaskRunError` after the project-existence check — no AI provider is installed/configured anywhere in this project (no `@anthropic-ai/sdk`, `openai`, `ai` package, or API key in any env file), and per `ai-workflow-rules.md` ("do not invent product behavior that is not defined in the context files," "split work that combines... real-time canvas state and database persistence") actually implementing the generation call + Liveblocks room write + spec persistence is a separate, larger unit than "add these task files." Verified via `tsc --noEmit` and `eslint trigger/` (both clean). **Still stub-only, untouched by 22-design-agent-api** — see that unit's notes for why a third, separate task file was added instead of building on these two.

- 22-design-agent-api: `prisma/models/task-run.prisma` (new file, alongside the existing `project.prisma` — kept separate since `TaskRun` has no Prisma relation to `Project`, just a plain `projectId` string per the spec's literal field list) — `TaskRun` (`id` cuid, `runId` unique, `projectId`, `userId`, `createdAt` only, indexes on `runId` and `[userId, projectId]`, exactly the spec's field/index list, nothing added). This is the first model backing `architecture-context.md`'s Storage Model line 26 ("task run records" in PostgreSQL), previously documented but not yet implemented. Migrated via `prisma migrate dev --name add_task_run` and regenerated the client.
- 22-design-agent-api: `trigger/design-agent.ts` (new) — `designAgent`, id `design-agent`, payload `{prompt, roomId}` per spec, logs the payload via `logger.log` and returns it unchanged (the literal "log or echo the input for now... don't add AI logic yet"). This is a **third, deliberately separate task file** from the pre-existing `trigger/generate-design.ts`/`trigger/generate-spec.ts` stubs (see the "In Progress" entries above) rather than a rebuild of `generate-design`: the spec named `trigger/design-agent.ts` explicitly with a different, narrower payload shape (`{prompt, roomId}`, no `canvas`/no `AbortTaskRunError` project-existence check), and `ai-workflow-rules.md`'s "do not invent product behavior not defined in the context files" argues against silently merging or renaming the older stubs as a side effect of this unit. All three task files coexist for now — reconciling them (or wiring the API route below to `generate-design` instead) is unresolved, flagged under Open Questions.
- 22-design-agent-api: `app/api/ai/design/route.ts` (new) — `POST` validates `{prompt, roomId, projectId}` (all three required strings, matching the spec's literal "accept the design prompt and required context (roomId, projectId)"), checks access via the existing `checkProjectAccess` helper (owner-or-collaborator, same helper `canvas`/`collaborators` routes use — not owner-only, since design generation should be usable by any room participant, consistent with every other canvas-adjacent route), triggers `design-agent` via `tasks.trigger<typeof designAgent>(...)` (type-only import of the task, per the SDK's "don't import the task instance into backend code" rule), creates a `TaskRun` row (`runId: handle.id`, `projectId`, `userId: identity.userId`), and returns `{runId}`.
- 22-design-agent-api: `app/api/ai/design/token/route.ts` (new) — `POST` accepts `{runId}`, looks up the `TaskRun` row by `runId` (`404` if missing), checks `taskRun.userId === identity.userId` (`403` if not — this is the literal "verify ownership using the TaskRun record" step, and satisfies `code-standards.md`'s "treat ownership and run IDs as verified before any token issuance"), then mints a Trigger.dev Public Access Token scoped to only that run (`auth.createPublicToken({ scopes: { read: { runs: [runId] } } } )`, imported as `triggerAuth` to avoid shadowing Clerk's own `auth()` used elsewhere in the codebase) and returns `{token}`. Deliberately does not re-check `checkProjectAccess` against the project — the spec's ownership model for this route is explicitly the `TaskRun` record (who triggered this specific run), not general project access, so a collaborator who left the project after triggering a run still can't be locked out of reading a run they started, and a current collaborator who didn't trigger the run can't read someone else's in-flight generation.
- 22-design-agent-api: verified via `tsc --noEmit` (clean, after `next typegen`), `eslint` (clean on all new files), `npm run build` (passes; both new routes registered), and — the first live verification in this project to actually exercise a real Trigger.dev run end to end — ran `npx trigger.dev@4.5.10 dev` against the real `TRIGGER_SECRET_KEY`/`TRIGGER_PROJECT_REF` already in `.env.local`, then drove the two new routes with the established `@clerk/backend` throwaway-user + Bearer-token pattern (owner + an unrelated stranger, real Prisma project row). Confirmed: unauthenticated `POST` on both routes → `401`; missing fields → `400`; a stranger with no project access → `404` on the design route; the owner's `POST /api/ai/design` → `200 {runId}` with a real Trigger.dev run ID, and a matching `TaskRun` row was created in Postgres; the stranger requesting a token for that run → `403`; a nonexistent `runId` → `404`; the owner's own token request → `200 {token}` with a real JWT-shaped string; **that minted token was then used to call Trigger.dev's real runs API directly (`GET https://api.trigger.dev/api/v3/runs/{runId}` with the token as a Bearer header)**, which returned `200` with `taskIdentifier: "design-agent"` and a real run status — confirming the token is genuinely scoped and functional, not just shaped correctly. The `trigger dev` process's own log independently showed both test runs (from this pass and an earlier one) reach `Success`, confirming `design-agent`'s `run()` actually executed and returned. All test users/sessions/the project/`TaskRun` rows were cleaned up afterward; the temporary verification script and the `trigger dev` process started for this session were both removed/stopped.
- 22-design-agent-api bugfix (found during live verification, not in the original spec): the first verification pass 500'd on `prisma.taskRun.create` inside the already-running `next dev` server with `TypeError: Cannot read properties of undefined (reading 'create')` — `prisma.taskRun` was `undefined` even though `prisma generate` had already run successfully. Root cause: the long-running `next dev` process (started before this session) had already imported the previous build of `app/generated/prisma`'s client module into memory; regenerating the client on disk doesn't hot-reload an already-loaded Node module for a route handler outside the Next.js file-watcher's HMR boundary (this generated-client directory isn't treated like app-tree source). Fixed by restarting `next dev` — this is the same class of issue 08-editor-workspace-shell's Session Notes already flagged for a route-manifest cache after a dynamic-segment rename; the general lesson (restart the dev server after any Prisma schema/client change made mid-session) is now confirmed to apply here too, not just to routing changes.

- 23-design-agent-logic: `liveblocks.config.ts`'s `Storage` type — left as an empty `Record<string, never>` placeholder since 10-liveblocks-setup — is now typed to match what `@liveblocks/react-flow`'s `useLiveblocksFlow` actually writes: `{ flow: LiveObject<{ nodes: LiveMap<string, LiveObject<{id,type,position,width,height,data: CanvasNodeData}>>, edges: LiveMap<string, LiveObject<{id,source,target,data: CanvasEdgeData}>> }> }`. This was necessary, not cosmetic: `@liveblocks/node`'s `Liveblocks.mutateStorage(roomId, ({root}) => ...)` types `root` against this same global `Storage` declaration, and `trigger/design-agent.ts` needs `root.get("flow")` to be properly typed to mutate the canvas safely from the backend. Confirmed via `@liveblocks/react-flow`'s own `.d.ts` that its exported `useLiveblocksFlow` hook does *not* itself depend on this global type (it's generically parameterized by the caller's `N`/`E` types instead), so widening `Storage` here doesn't affect `components/editor/canvas.tsx`'s existing usage at all. `RoomEvent` (also an empty placeholder since 10-liveblocks-setup) is now typed as the literal AI status event shape (see below).
- 23-design-agent-logic: `components/editor/workspace-shell.tsx` now owns the `LiveblocksProvider`/`RoomProvider` connection directly (moved out of `components/editor/canvas-room.tsx`, which now only contains the `RoomConnectionGuard`/`ErrorBoundary`/`ClientSideSuspense`/`Canvas` chain) — necessary because `AiSidebar` needs the same room's presence/event hooks (for the AI's presence and the shared status feed) as a *sibling* of the canvas, and a `RoomProvider` can only be established once, above every consumer that needs it; `AiSidebar` was previously rendered outside any Liveblocks context entirely. `RoomProvider` gained an explicit `initialStorage` (a `flow: LiveObject<{nodes: LiveMap, edges: LiveMap}>` factory) — TypeScript now requires this prop now that `Storage` is a real (non-empty) type; this also removes the extra round-trip `useLiveblocksFlow`'s own lazy `setInitialStorage` effect would otherwise need on a genuinely new room.
- 23-design-agent-logic: `lib/liveblocks.ts` gained `AI_AGENT_USER_ID` (`"ai-agent"`), `AI_AGENT_NAME` (`"Ghost AI"`), `AI_AGENT_COLOR` (`#6457f9`, the existing "AI accent" token from `ui-context.md`) — the identity `trigger/design-agent.ts` sets via `Liveblocks.setPresence()`. Because this is a normal `UserMeta`-shaped presence entry, it shows up in `useOthers()` automatically, so **`components/editor/presence-avatars.tsx` and `components/editor/presence-cursors.tsx` needed no new component** — the AI gets a real avatar (initials "GA" in the AI accent color, since it has no `avatar` image) and a real moving cursor for free, exactly like a human collaborator, per this unit's "reuse existing Liveblocks flow and presence patterns instead of creating new ones" instruction. Both components gained a small `thinking` extension: `CollaboratorAvatar` now renders a pulsing `bg-ai` dot when `other.presence.thinking` is true, and `PresenceCursors`' name badge appends "· thinking…" — `Presence.thinking` (renamed from `isThinking` in 19-presence-avatars-cursor) had been set but never consumed by any UI until now.
- 23-design-agent-logic: `trigger/design-agent.ts` rewritten in full. Flow: `setPresence(thinking: true)` → broadcast `"started"` → read the room's current canvas via `Liveblocks.getStorageDocument(roomId, "json")` (best-effort — falls back to an empty context on failure, doesn't fail the run) → broadcast `"processing"` → `generateObject` (Gemini) → apply the result to the room's Storage via `Liveblocks.mutateStorage()` → move the AI's cursor to the new content and broadcast `"complete"` with the model's one-sentence summary → in a `finally`, clear presence (`thinking: false`, `ttl: 2` — Liveblocks has no explicit "delete presence" call, so a 2-second TTL, its minimum, is what "clear AI presence when the task finishes" resolves to). Any failure broadcasts `"error"` with the message and throws `AbortTaskRunError` (not a retry — a retry would re-read the by-then-already-partially-mutated canvas and could double-apply changes that succeeded before the failure).
- 23-design-agent-logic: canvas mutation writes directly into the same `flow.nodes`/`flow.edges` `LiveMap`s `@liveblocks/react-flow` manages client-side (via `@liveblocks/node`'s `LiveObject`, matching the internal shape `useLiveblocksFlow`'s `toLiveblocksInternalNode`/`Edge` produce — confirmed by reading `node_modules/@liveblocks/react-flow/dist/lib/flow.js` and `shared.js`), so every connected client sees the AI's changes the instant they land, through the same collaborative flow every manual node/edge edit already goes through — no new state system, no polling, no client-side apply step. New AI-added node/edge ids are prefixed `` `ai-${runId}-${modelId}` `` (same collision-avoidance precedent 18-starter-template's template-import id remapping established) so concurrent/repeated runs can never collide with each other or with manually-created nodes. New nodes are offset (same `existingBounds.maxX + gap` convention `canvas.tsx`'s `handleImportTemplate` uses, gap = 120 flow units) so they always land beside existing canvas content, never on top of it — the literal "layout and spacing rules" this unit's spec asked for.
- 23-design-agent-logic, schema design (the hard part — two earlier approaches both failed live against this project's real Gemini key before landing on this one): the model's output shape is **seven separate, homogeneous zod arrays** (`addNodes`, `moveNodes`, `resizeNodes`, `updateNodeData`, `deleteNodes`, `addEdges`, `deleteEdges` — one per the spec's required action list) rather than one polymorphic `actions` array. A `z.discriminatedUnion("action", [...])` made Gemini's structured-output mode fail outright at the API level ("No object generated: response did not match schema"). Replacing it with a single flat object (every field but `action`/`id` optional, "which fields matter" explained only in prose/`.describe()`) *did* validate, but the model reliably produced garbage under it — e.g. four near-duplicate `add_node` entries for the same id, most missing `color` entirely — because it had no schema-level signal for which fields a given `action` value actually required. Splitting into seven arrays, each with genuinely required fields (only `label`/`color` stay optional, on `updateNodeData`/`addEdges`, where they're optional in the product sense too), fixed this completely — confirmed by 4 consecutive live runs against the real API, each applying every single requested action (see Session Notes).
- 23-design-agent-logic: the model id is `"gemini-flash-latest"`, not a pinned version — confirmed live that this project's actual Google AI key already rejects `"gemini-2.5-flash"` ("This model ... is no longer available to new users"), even though that exact model is still returned by `models.list`. `"-latest"` always resolves to Google's current default Flash model, so this task doesn't need a code change every time Google sunsets a pinned version. The Google provider is constructed with `createGoogleGenerativeAI({apiKey: process.env.GOOGLE_AI_API_KEY})` inside `run()`, not the default `google` singleton (which reads `GOOGLE_GENERATIVE_AI_API_KEY`) and not at module scope — **`23-design-agent-logic.md`'s spec said `GEMINI_API_KEY` is already in `.env.local`, but the key actually present there is named `GOOGLE_AI_API_KEY`**; used what's actually there rather than trusting the stale spec text or renaming the real env file. Constructing the client lazily inside `run()` (not at module scope) mirrors `lib/liveblocks.ts`'s existing lazy-client pattern, for the same reason documented there (10-liveblocks-setup's eager-construction/`next build` bugfix) — cheap insurance against the same class of bug recurring for a second external SDK client, even though it wasn't actually observed for this one.
- 23-design-agent-logic: `components/editor/ai-sidebar.tsx`'s `AiArchitectTab` now takes `roomId`/`projectId` props (same value in practice, per 07-wire-editor-home's "project ID and Liveblocks room ID stay aligned" invariant) and actually calls `POST /api/ai/design` on send (still appending the user's own bubble locally first, unchanged from 20-ai-sidebar-shell). It does **not** call `POST /api/ai/design/token` or subscribe via `useRealtimeRun`, even though 22-design-agent-api's Next Up note suggested wiring that in — deliberate: that token is scoped to whoever triggered the run (`TaskRun.userId`, per 22's own ownership model), but the spec's "publish AI activity to the shared status feed so all users see progress" needs every room participant to see it, not just the sender. Chat status bubbles are instead driven entirely by `useEventListener` on the Liveblocks `RoomEvent` `trigger/design-agent.ts` broadcasts — reaches every connected client in the room for free, with no per-user token plumbing. `POST /api/ai/design/token` (and the whole run-subscription path it was built for) remains unused by the frontend; it's still exercised and correct per 22's own live verification, just not this unit's chosen mechanism. A failed `fetch` (network error or non-2xx) appends a local-only error bubble immediately, separate from the task's own `"error"` broadcast for failures that happen after triggering.
- 23-design-agent-logic: `package.json` gained `zod` (`^4.4.3`) as a direct dependency — it was already present in `node_modules` (a transitive dependency, likely of `ai`'s own peer chain) but never installed as a real project dependency; `trigger/design-agent.ts` now imports it directly for the response schema, so an implicit hoisted resolution wasn't good enough to depend on.
- 23-design-agent-logic: verified via `tsc --noEmit` (clean), `eslint .` (clean), `npm run build` (passes, all routes still register correctly) — plus, for the first time in this project, a genuinely live, end-to-end `trigger dev` + real Gemini + real Liveblocks run (not just a stub echo): using the established `@clerk/backend` throwaway-user pattern and a temporary Playwright/Chromium pass (symlinked from a prior session's scratchpad install, removed after use, same recurring gap noted since `02-editor`). Confirmed via 4 consecutive real runs against a live Postgres/Liveblocks/Trigger.dev/Gemini stack: `POST /api/ai/design` → real run → canvas mutated with correctly-shaped nodes (valid shapes/palette colors, sized via `NODE_DEFAULT_SIZES`, positioned beside — never overlapping — a pre-seeded existing node) and edges (valid source/target, connecting through both pre-existing and newly-added nodes) — 100% of requested actions applied on every one of the last 3 runs, after the schema redesign above. Browser pass additionally confirmed, on the same live connection with no page reload: the user's own prompt bubble renders immediately; `"started"`/`"processing"` status bubbles arrive live via the broadcast `RoomEvent`; the AI genuinely appears as a `title="Ghost AI · thinking…"` presence avatar during the run (not asserted from server logs — read directly off the DOM); the AI's presence disappeared again within its 2s clear-TTL after the run finished; the final `"complete"` summary bubble arrived; and the new nodes/edges rendered on the canvas live. Real project rows, `TaskRun` rows, and Clerk users/sessions were all cleaned up after every run; the temporary verification scripts and the scratchpad Playwright symlink were removed afterward.

- 24-ai-presence-state: `types/tasks.ts` (new) — `AI_STATUS_FEED_STATUSES`/`AiStatusFeedStatus` and `aiStatusFeedPayloadSchema` (zod: `status` enum, `text` optional string, `runId` string), the single source of truth both the publisher (`trigger/design-agent.ts`) and the subscriber (`components/editor/ai-sidebar.tsx`) validate against — satisfies this unit's literal "define the feed payload schema in `types/tasks.ts`" / "the payload should support an optional `text` field" / "validate incoming messages before displaying them."
- 24-ai-presence-state: reused (per the spec's explicit "check the existing Liveblocks setup ... before creating parallel realtime state") rather than replaced 23-design-agent-logic's existing `ai-status` `RoomEvent` broadcast mechanism — this codebase's `liveblocks-best-practices` skill has no dedicated "Feeds" primitive/reference, so "create or reuse a Liveblocks feed named `ai-status-feed`" was read as: rename the existing broadcast-event channel to that literal name and give it the schema above, not build a new mechanism. `liveblocks.config.ts`'s `RoomEvent` is now `{ type: "ai-status-feed" } & AiStatusFeedPayload` (was `{ type: "ai-status", status, message, runId }`); `trigger/design-agent.ts`'s `publishStatus` broadcasts the renamed shape (`message` param renamed `text` to match).
- 24-ai-presence-state: `components/editor/ai-sidebar.tsx`'s `AiArchitectTab` now tracks only the single latest validated `ai-status-feed` message (`latestStatus` state, replaced on every event — never appended to an array), not the full event history, per the explicit "don't show full feed history" / "show only the most recent status message" scope rules. This drives a small shared status-indicator row (spinner + `latestStatus.text`) rendered between the message list and the input, visible to every participant in the room (it's driven by the room-wide `useEventListener`, not the sender's own local state) — shown only while `isGenerating` (derived as `status === "started" || "processing"`), so it never persists or blocks/dims the rest of the sidebar. The final `"complete"`/`"error"` message is still appended once to the chat transcript (unchanged mechanism from 23, just now gated to those two terminal statuses instead of every intermediate progress event) so the AI's actual reply to the user's prompt is still visible after the indicator disappears.
- 24-ai-presence-state: `isGenerating` (`isSending` OR the shared feed status) now also disables the `Textarea` itself (previously only the send button was disabled) and swaps the send button's `Send` icon for a spinning `Loader2` while true — satisfies "disable the chat input while generation is active" / "show a loading state on the send button." A malformed/incomplete event (missing `runId`, the required field) is silently dropped via `aiStatusFeedPayloadSchema.safeParse` failing, confirmed live (see verification below) rather than assumed from the code.
- 24-ai-presence-state: `components/editor/presence-cursors.tsx`'s cursor name badge now renders an actual small spinner (`lucide-react`'s `Loader2`, `animate-spin`) when `other.presence.thinking` is true, replacing the previous "· thinking…" text suffix — the literal "show a small spinner ... hide the spinner when thinking is false or missing." `presence-avatars.tsx`'s existing pulsing-dot indicator for the same `presence.thinking` field was left untouched (out of this unit's scope, which named cursors specifically).
- 24-ai-presence-state: verified via `tsc --noEmit` (clean), `eslint .` (clean on all changed/new files), `npm run build` (passes, all routes still register), and a live Playwright pass against the project's existing `next dev` server (port 3000, already running — a second `next dev` instance refused to start on a different port, confirmed expected via its own "Another next dev server is already running" message) using the established `@clerk/backend` throwaway-user + Bearer-token pattern. Rather than running a full live Gemini/Trigger.dev generation to exercise the feed, drove `@liveblocks/node`'s `Liveblocks.broadcastEvent`/`setPresence` directly against the real room from a small script — the exact same calls `trigger/design-agent.ts` itself makes — which is a faster and equally faithful way to test the UI's reaction to the feed/presence contract in isolation. Confirmed: idle state has an enabled textarea and a plain `Send` icon; broadcasting a `"processing"` event disables the textarea, swaps the send button to a spinning `Loader2`, and shows the shared status indicator with the event's `text`; a second, malformed event (missing `runId`) rendered nowhere (validation confirmed, not just assumed); broadcasting `"complete"` re-enables the input, removes the status indicator, and appends the summary as a chat bubble; setting a synthetic second participant's presence to `{cursor, thinking: true}` showed a spinner in their cursor badge, and flipping `thinking` to `false` removed it. Screenshots confirmed the visual result at each step. `playwright` was symlinked into `node_modules` from a prior session's scratchpad install (same recurring gap noted since `02-editor`) and removed afterward, along with the temporary verification script and the throwaway Clerk user/Prisma project.

- 25-sidebar-chat-feed: discovered this session that the Liveblocks SDK version already installed (`^3.23.1`) ships a real, room-scoped **Feeds** primitive — `Feed`/`FeedMessage` types, client hooks `useFeeds`/`useFeedMessages`/`useCreateFeed`/`useCreateFeedMessage`/`useUpdateFeedMessage`/`useDeleteFeedMessage`/`useDeleteFeed`/`useUpdateFeedMetadata`, and server-side `Liveblocks.createFeed`/`getFeed`/`getFeedMessages`/`createFeedMessage`/etc. (`@liveblocks/node`). This isn't documented in this project's `liveblocks-best-practices` skill (its quick-reference list has no "Feeds" entry) and wasn't known when 24-ai-presence-state wrote "this codebase's `liveblocks-best-practices` skill has no dedicated 'Feeds' primitive/reference" — confirmed instead by reading the compiled `.d.ts` files directly (`node_modules/@liveblocks/core/dist/index.d.ts`, `node_modules/@liveblocks/react/dist/room-DUr5FIYB.d.ts`, `node_modules/@liveblocks/node/dist/index.d.ts`). Unlike `ai-status-feed` (24's reused RoomEvent broadcast, which is transient and not replayed to late joiners), a real Feed persists its messages server-side and replays full history to anyone who joins the room later — the correct primitive for this unit's literal ask ("create or reuse a Liveblocks feed named `ai-chat`," "render chat messages in order").
- 25-sidebar-chat-feed: `types/tasks.ts` gained `CHAT_FEED_ID` (`"ai-chat"`), `CHAT_MESSAGE_ROLES` (`["user"] as const` — no AI-generated replies yet per this unit's explicit scope limit, but the field exists so a future unit can add assistant messages to the same feed without a schema change), and `chatMessageSchema` (zod: `sender`, `role`, `content`, `timestamp` — the literal shape the spec asked for). `liveblocks.config.ts`'s global `Liveblocks` interface gained `FeedMessageData: ChatMessage` (the `ExtendableTypes` override `@liveblocks/core` reads to type `FeedMessage<FMD>.data` project-wide), following the same "declare the type, both publisher and subscriber validate against types/tasks.ts" pattern `AiStatusFeedPayload`/`RoomEvent` already established in 24.
- 25-sidebar-chat-feed: feeds have no `getOrCreateRoom`-style convenience — `Liveblocks.createFeed` on an already-existing feed rejects with a 409 `LiveblocksError` (confirmed by reading `node_modules/@liveblocks/node/dist/index.js`, and by this unit's own doc-comment elsewhere in the SDK: "If the thread already exists, a `LiveblocksError` will be thrown with status code 409" — the same REST convention applies to feeds). `lib/liveblocks.ts` gained `ensureChatFeedExists(liveblocks, roomId)`, which calls `createFeed` and swallows a 409, and `app/api/liveblocks-auth/route.ts` calls it right after the existing `getOrCreateRoom` — every client that connects to a room now idempotently ensures the `ai-chat` feed exists there first, so no client ever races to create it and no client can hit `FEED_NOT_FOUND` when it later tries to send.
- 25-sidebar-chat-feed: `components/editor/ai-sidebar.tsx` gained a third `RoomChatTab`, wired as a new "Chat" tab between the existing "AI Architect" and "Specs" tabs. Reads via `useFeedMessages(CHAT_FEED_ID)` / sends via `useCreateFeedMessage()`, both imported from `@liveblocks/react` (the **non-suspense** entry point, not `/suspense`) — deliberately, because `AiSidebar` is a direct child of `RoomProvider` in `components/editor/workspace-shell.tsx` with no `ClientSideSuspense` wrapper around it (unlike the canvas), and the suspense-variant of these two hooks throws a promise while loading. The regular `{messages, isLoading, error}` hooks need no such wrapper, per the `liveblocks-best-practices` skill's `suspense-vs-regular-hooks` reference — confirmed this mixing (suspense-import `RoomProvider`/`useEventListener` alongside non-suspense-import `useFeedMessages`) is supported, since both entry points share the same underlying room context bundle (`room-DUr5FIYB.js`).
- 25-sidebar-chat-feed: every message read from the feed is validated via `chatMessageSchema.safeParse(message.data)` before rendering (dropped silently if invalid), the same "validate before trusting" precedent 24-ai-presence-state's `aiStatusFeedPayloadSchema.safeParse` established for `ai-status-feed` — confirmed live (see Session Notes) that a malformed feed message posted directly via the Liveblocks REST API renders nowhere, while valid messages around it are unaffected. `sender` is stored as the sending user's Clerk display name (`user.fullName ?? user.primaryEmailAddress?.emailAddress ?? "Guest"`, from `@clerk/nextjs`'s client-side `useUser()`) captured at send time, not looked up live from room presence — necessary because a Feed persists messages from participants who may have since disconnected, and `useOthers()`/presence only has currently-connected users. Messages render uniformly (sender name + timestamp header, then a bubble) rather than left/right-aligned by "is this mine" — deliberately avoids needing a reliable identity comparison (two participants could share a display name) for a distinction the spec never actually asked for.
- 25-sidebar-chat-feed: send button uses `bg-brand` (the app's general accent), not `bg-ai` — the Architect tab's `bg-ai` signals "this talks to the AI," which this new Chat tab explicitly must not do (this unit's own scope limits: "don't trigger backend AI tasks," "don't mix chat messages with status messages"). Enter-to-send / Shift+Enter-for-newline and the disabled-while-sending pattern both mirror `AiArchitectTab`'s existing input exactly, per the spec's "use the existing sidebar input and send button" (read as: reuse the same interaction/component conventions, not the literal same tab instance, since that instance's send button is wired to `/api/ai/design`). A failed send (`createFeedMessage` rejects or times out — confirmed live via a real 5s-timeout offline test, see Session Notes) shows a small `text-error` line ("Message failed to send. Try again.") below the input and leaves the typed text in place rather than clearing it, so nothing the user typed is lost.
- 25-sidebar-chat-feed: verified via `tsc --noEmit` (clean), `eslint` (clean on all changed/new files), `npm run build` (passes, all routes still register), and a live Playwright pass against the project's already-running `next dev` server (port 3000, real `LIVEBLOCKS_SECRET_KEY`/`CLERK_SECRET_KEY`/`DATABASE_URL`) with two real Clerk users (owner + a seeded `ProjectCollaborator`) in the same room simultaneously. Confirmed: empty state shows before any message; the owner's sent message appears live on the collaborator's screen with no reload (and vice versa for a reply); the owner's textarea clears immediately after a successful send; the sender's display name renders on the *other* participant's screen (not just the sender's own); reloading the owner's page shows both messages still present — real Feed persistence, not local-only React state (unlike 20-ai-sidebar-shell's original chat UI, which was local-state-only); switching to the AI Architect tab shows none of the chat content, confirming the two are genuinely separate channels; a message posted directly via the Liveblocks REST API with a schema-violating payload (`{foo: "..."}`) rendered nowhere in the UI while the real messages around it stayed visible, confirming client-side validation actually runs (not just assumed from the code); and, in a separate offline-network run, a send that can't complete shows the "Message failed to send" error text within its 5s SDK-level timeout and leaves the unsent text in the input. All Clerk test users, the temporary project (and its `ProjectCollaborator` row), the temporary verification scripts, and the scratchpad Playwright symlink were removed afterward.

- 26-design-agent-frontend: this is the first unit in the project to actually call `@trigger.dev/react-hooks`'s `useRealtimeRun` from the frontend — `POST /api/ai/design/token` and the run-subscription path had existed since 22-design-agent-api but were unused until now (23's Completed notes explained why the shared-status-feed requirement pointed at a Liveblocks `RoomEvent` broadcast instead at the time). `app/api/ai/design/route.ts` now also mints a Trigger.dev Public Access Token (the same `triggerAuth.createPublicToken({scopes:{read:{runs:[taskRun.runId]}}})` call already proven in `/api/ai/design/token`) and returns it alongside `runId` in one response, per this unit's literal "read `{runId, publicToken}` from the response" — `/api/ai/design/token` itself is untouched and still correct, just doubly unused by the frontend now.
- 26-design-agent-frontend, the naming-collision decision (flagged to the user, not resolved silently, per `ai-workflow-rules.md`'s "don't resolve ambiguous requirements as a side effect of other work"): this spec's literal "push the user message to the `ai-chat` feed" / "push a final AI message to `ai-chat`" collided with 25-sidebar-chat-feed's original design, which named that exact feed for **human-to-human room chat only**, deliberately kept separate from AI conversation. A Liveblocks feed message has no "channel" field, so there was no way to satisfy the literal spec text without merging the two. **User's explicit decision: reuse `ai-chat` literally** — see Architecture Decisions below for the resulting behavior.
- 26-design-agent-frontend: `types/tasks.ts`'s `CHAT_MESSAGE_ROLES` widened from `["user"]` to `["user", "assistant"]` — exactly the extension 25 had already anticipated ("the field exists so a future unit can add assistant messages to this same feed without a schema change"). No other schema change was needed.
- 26-design-agent-frontend: `components/editor/ai-sidebar.tsx`'s `AiArchitectTab` rewritten. On submit: pushes the user's prompt to the `ai-chat` feed (`sender` = the sender's real Clerk display name, `role: "user"`) via the same `useCreateFeedMessage`/`useFeedMessages(CHAT_FEED_ID)` pair `RoomChatTab` already established in 25; then `POST /api/ai/design` with `{prompt, roomId, projectId}` (the API still requires `projectId`, unchanged from 22 — the spec's literal `{prompt, roomId}` was read as "at minimum these," not "exclude projectId"); stores the returned `runId`/`publicToken` in local state. `useRealtimeRun<typeof designAgent>(runId, {accessToken: publicToken, enabled: !!runId && !!publicToken, skipColumns: ["payload"]})` drives everything from there via its `onComplete` callback: reads `completedRun.output.summary` on success or `completedRun.error?.message` on failure, pushes that as a `role: "assistant"` feed message from "Ghost AI", then clears `runId`/`publicToken` — which is also what flips `isGenerating` (`isSending || !!runId`) back to `false`, satisfying "reset loading + run state." `isGenerating` disables the textarea and swaps the send button to a spinner; the existing `ai-status-feed` `useEventListener` (unchanged mechanism from 23/24) now only drives the compact status strip's text, gated on `!!runId` — it no longer appends anything to a local transcript, since the transcript itself is now just `chatMessages` from the feed (the same derivation `RoomChatTab` uses: `useFeedMessages` → `chatMessageSchema.safeParse` → sort by timestamp).
- 26-design-agent-frontend, colors: the spec's literal "green accent (`#62C073`)" for the user's chat bubble, the submit button, and the status strip's accent is not a new color — `#62C073` is the existing "Green" pair's text value in `types/canvas.ts`'s `NODE_COLORS` (`NODE_COLORS[6].text`, documented in `ui-context.md`'s Node Color Palette). Referenced from that array (`AI_CHAT_ACCENT` in `ai-sidebar.tsx`) rather than re-hardcoded, so it can't drift from the palette. Applied via inline `style` (background/color for the user bubble and send button, `color` for the status strip's icon/text) since Tailwind has no static utility for a data-driven palette color — same pattern `node-color-toolbar.tsx` already uses for per-swatch colors. The AI/assistant bubble keeps its existing `bg-elevated`/`text-ai-text` dark styling from 20-ai-sidebar-shell, matching this spec's "AI: dark background, light text." The status strip uses `bg-elevated` (dark base) with the same green accent (text/spinner), satisfying "Dark base + green accent."
- 26-design-agent-frontend: `AI_SENDER_NAME` ("Ghost AI") is a small local constant in `ai-sidebar.tsx`, not imported from `lib/liveblocks.ts`'s existing `AI_AGENT_NAME` — that module imports the server-only `@liveblocks/node` SDK, which must never end up in a `"use client"` component's bundle. A one-line duplicated string was judged cheaper than that risk.
- 26-design-agent-frontend: verified live end-to-end — started `npx trigger.dev@4.5.10 dev` against the real `TRIGGER_SECRET_KEY`/`GOOGLE_AI_API_KEY`, reused a prior session's scratchpad Playwright/Chromium install (symlinked into `node_modules/playwright`+`node_modules/playwright-core`, removed after use), and drove a real signed-in browser session against a throwaway `@clerk/backend` user and a real Prisma project. This unit's new `useUser()`-dependent send path (needed for the sender's real display name) ruled out the Bearer-token Playwright pattern used by most other units (per 25's Session Notes, `useUser()` never resolves under a global `Authorization` header, since Clerk's own JS fails to load) — used 25's real-cookie-sign-in technique instead: drove the actual `/sign-in` form, including the `+clerk_test@example.com` / fixed-OTP-`424242` device-verification interstitial flow. Confirmed: the textarea disabled immediately after clicking Send; the user's bubble rendered with computed `background-color: rgb(98, 192, 115)` (exactly `#62C073`); the compact status strip appeared during the run with computed colors `rgb(24, 24, 28)` background (`bg-elevated`) and `rgb(98, 192, 115)` text (the same green) — confirmed via a proper `waitFor({state:"visible"})` poll, not a bare `isVisible()` call, after a first attempt raced a fast-completing run and produced a false negative; once the run completed, an assistant-role "Ghost AI" bubble appeared, the textarea re-enabled, and the status strip disappeared; reloading the page showed both the user's prompt and Ghost AI's reply still present (real feed persistence — satisfies "Chat updates appear across multiple sessions"); switching to the "Chat" tab showed the same design prompt (confirming the merge decision above actually took effect, not just in code); the canvas gained 2 real AI-generated nodes, confirming `design-agent` genuinely ran rather than being mocked. Hit the same `#liveblocks-badge`-intercepts-clicks issue documented in 23's Session Notes; worked around it with the same `locator.evaluate(el => el.click())` technique. `tsc --noEmit`, `eslint .`, and `npm run build` were all clean before this live pass (all API routes, including the still-present `/api/ai/design/token`, registered correctly). The throwaway Clerk user, the Prisma project, the temporary script, the `trigger dev` process, and the Playwright/Chromium symlinks were all removed afterward.

- 27-spec-generation-flow: `trigger/generate-spec.ts` implemented for real (was an unimplemented stub) — `generateSpec`, a `schemaTask` (payload validated with Zod, per this unit's explicit "validate input with Zod" rule, unlike `design-agent.ts`'s plain `task`). Payload is `{projectId, roomId, chatHistory, nodes, edges}`; `nodes`/`edges` are validated against a loose React-Flow-shaped schema (`id`, optional `position`/`width`/`height`, optional `data: {label, color, shape}`) rather than the flattened shape `design-agent.ts` reads from Liveblocks Storage — this unit's spec has the API route accept `nodes`/`edges` directly in the request body (not read server-side from Storage), so the task validates whatever shape the (future, out-of-scope) frontend sends. `chatHistory` reuses `chatMessageSchema` from `types/tasks.ts` unchanged.
- 27-spec-generation-flow: the task builds a prompt from formatted (not raw-JSON) node/edge/chat summaries and calls `generateText` (not `generateObject` — output is plain prose Markdown, not structured data) against `google("gemini-flash-latest")`, the same model id `design-agent.ts` already confirmed live against this project's real key. A `stripCodeFence()` helper defensively unwraps a \`\`\`markdown fence around the whole response — confirmed live this was necessary at least once during verification, Gemini doesn't reliably honor "no code fence" as a plain instruction. Task output is the plain Markdown string itself (not wrapped in `{spec: ...}`), per this unit's literal "keep the task output as plain Markdown."
- 27-spec-generation-flow: "updates run metadata/status for realtime tracking" is implemented via Trigger.dev's own `metadata.set("status", ...)`/`metadata.set("statusText", ...)` calls (`started` → `processing` → `complete`/`error`) — deliberately not a Liveblocks `broadcastEvent` to `ai-status-feed` like `design-agent.ts` uses. This is a genuine mechanism difference, not an oversight: `design-agent.ts` broadcasts to Liveblocks because multiple room participants need to see AI presence/status on the shared canvas in real time regardless of who triggered the run; spec generation has no canvas presence to show and its literal spec text says "run metadata," which is Trigger.dev's own realtime-run-metadata feature (readable via `useRealtimeRun`'s `run.metadata`, per the `trigger-realtime-and-frontend` skill) — the correct mechanism when only the triggering client (a future frontend unit) needs live status for one specific run, not the whole room.
- 27-spec-generation-flow: `POST /api/ai/spec` (new) validates the request body against `generateSpecRequestSchema` (exported from `trigger/generate-spec.ts` — `generateSpecPayloadSchema` minus `projectId`, shared so the route and task validate against the same shape rather than duplicating it), authenticates via `getCurrentIdentity()`, then resolves project access from `roomId` alone via the existing `checkProjectAccess()` — never from a client-supplied `projectId`, satisfying this unit's explicit "do not trust a client-supplied projectId" (the request body has no `projectId` field at all, so there's nothing to ignore-and-resolve-instead; the schema itself makes the wrong shape unrepresentable). `project.id` (server-resolved) is what's actually passed to the triggered task and stored on the `TaskRun` row. Mirrors `/api/ai/design`'s existing `tasks.trigger` + `prisma.taskRun.create` pattern, but does not mint a Trigger.dev public token inline (unlike `/api/ai/design`'s post-26 addition) — this unit's spec keeps token-minting as a separate, explicit second route.
- 27-spec-generation-flow: `POST /api/ai/spec/token` (new) — same ownership-check shape as the pre-existing `/api/ai/design/token` (`TaskRun.userId !== identity.userId` → `403`, unknown `runId` → `404`), but passes `expirationTime: "1hr"` to `auth.createPublicToken()`, satisfying this unit's explicit 1-hour-expiration requirement (`/api/ai/design/token` has no expiration override and so uses Trigger.dev's 15-minute default — that route is unchanged, this is a deliberate per-route difference, not applied retroactively).
- 27-spec-generation-flow: verified via `tsc --noEmit` (clean), `eslint` (clean), `npm run build` (passes, both new routes registered as `/api/ai/spec` and `/api/ai/spec/token`), and a live end-to-end pass against the project's already-running `next dev` and `trigger dev` processes (confirmed `generate-spec` was registered on the current worker via the `get_current_worker` MCP tool before testing) using the established `@clerk/backend` throwaway-user + Bearer-token pattern (this unit's routes don't depend on `useUser()`/client-side Clerk state, so the simpler Bearer pattern was sufficient, unlike 25/26's chat features): unauthenticated request `401`; owner trigger with a real 2-node/1-edge canvas + one chat message `200` with a `runId`; a matching `TaskRun` row created with the correct server-resolved `projectId`/`userId`; non-owner token request `403`; owner token request `200` with a real JWT (decoded and confirmed `exp - iat = 3600`, `scopes: ["read:runs:<runId>"]`); unknown `runId` token request `404`. Used `mcp__trigger__wait_for_run_to_complete` to confirm the real run actually completed with real Gemini-generated Markdown output (headed sections: Overview, Architecture Components, Data Flow, Open Questions, correctly describing the gateway→Postgres canvas and referencing the chat message) and run metadata showing `{status: "complete", statusText: "Spec ready."}`. All test users, the temporary project/TaskRun row, and the temporary verification script were removed afterward.
- 27-spec-generation-flow, testing note: the first verification pass reused one Clerk session token across the whole script and got spurious `401`s partway through (`owner POST /api/ai/spec/token -> 401`) — root cause was the token's own ~60s TTL elapsing across several sequential Clerk API round trips (two `createUser` calls plus session/token minting) before the token was even first used, not a bug in either route (the earlier calls with the same pattern, and the non-owner's freshly-minted token, both worked correctly). Fixed by minting a fresh token immediately before each request rather than reusing one for the whole script. Worth reusing this "mint per-request, not per-script" pattern for any future verification script with more than a couple of sequential authenticated calls.

- 28-spec-persistence-download: `prisma/models/project-spec.prisma` (new file, same one-model-per-file convention as `task-run.prisma`) adds `ProjectSpec` (`id`, `projectId`, `filePath`, `createdAt`, a `Project` relation with `onDelete: Cascade`, index on `projectId`) — metadata only, per this unit's explicit "do not store spec content in Prisma." `project.prisma`'s `Project` model gained the back-relation field `specs ProjectSpec[]` (required by Prisma for the one-to-many relation, same pattern as the existing `collaborators` field). Migration `20260811232434_add_project_spec` applied.
- 28-spec-persistence-download: `trigger/generate-spec.ts` now persists its own output immediately after generating it (inside the task's `run()`, not from a separate client-triggered save call — this task is the only place "a spec was generated" ever happens, and request handlers must stay thin per `code-standards.md`). Generates a `specId` via `randomUUID()` first so the same id can be used for both the Blob path and the `ProjectSpec.id` in one step (no create-then-update race): uploads the Markdown to Vercel Blob at `specs/{projectId}/{specId}.md` (`access: "private"`, `allowOverwrite: true`, `addRandomSuffix: false` — the exact same options `app/api/projects/[projectId]/canvas/route.ts`'s autosave already established for private blob writes, per this unit's "follow the same metadata + blob pattern used for canvas persistence"), then creates the `ProjectSpec` row with that blob URL as `filePath`. The task's return value changed from a bare `string` to `{spec, specId}` — `specId` is the only way anything downstream could ever reach the persisted spec (the download route needs it as a path param), and nothing in the current codebase consumed the old bare-string return (confirmed via a repo-wide grep — only `app/api/ai/spec/route.ts` imports `generateSpec`, and it only reads `handle.id`/the run id, never the task's output), so this was a safe, non-breaking shape change.
- 28-spec-persistence-download: `GET /api/projects/[projectId]/specs/[specId]/download` (new) — authenticates via `getCurrentIdentity()`, resolves project access via the existing `checkProjectAccess()` (owner or collaborator; returns a merged 404 for both "project doesn't exist" and "no access," reusing the exact same pattern the canvas `GET` route and the collaborators `GET` route already use for any-accessor reads, rather than the owner-only `findOwnedProject`'s distinct 403), then looks up the spec with `prisma.projectSpec.findFirst({where: {id: specId, projectId}})` — scoping by both fields in one query is what satisfies "verify the spec belongs to that project" (a real spec id from a *different* project 404s here even though it exists in the table). Fetches content via `@vercel/blob`'s `get(spec.filePath, {access: "private", useCache: false})` (same private-blob-read helper the canvas `GET` route uses — an unauthenticated raw `fetch` against a private blob URL 403s, so this authenticated helper is the only way to read the content) and streams it back as `Content-Type: text/markdown; charset=utf-8` with `Content-Disposition: attachment; filename="spec-{specId}.md"`.
- 28-spec-persistence-download: fixed a stale line in `context/architecture-context.md`'s AI Generation Model section — it still said generated specs are "saved to the filesystem," left over from before that same file's own Storage Model section (added at an earlier, unlogged point) already specified Vercel Blob at `specs/{projectId}/{specId}.md`. This was the literal conflict the Open Questions section had been tracking since 27-spec-generation-flow; corrected the stale line to match Storage Model (and this unit's actual implementation) rather than leaving two contradictory statements in the same file. `code-standards.md`'s "Canvas snapshots and generated specs belong in Vercel Blob; Prisma stores only the blob URL reference" was already consistent with this and needed no change.
- 28-spec-persistence-download: verified via `prisma validate` (clean), `prisma migrate dev` (applied cleanly) + `prisma generate`, `tsc --noEmit` (clean), `eslint` (clean on the new/changed files), and `npm run build` (passes, new download route registered as a dynamic route). No live end-to-end run was performed for this unit (would require triggering a real `generate-spec` run and then hitting the download route) — static verification only, per the explicit "do not add frontend or UI logic" scope limit meaning there's no in-app path to exercise this manually yet; a future unit wiring up the frontend should do a live pass covering both success and the not-found/forbidden cases.

- 29-spec-ui-integration: `types/project-spec.ts` (new, `ProjectSpec { id, filename, createdAt: Date }`) and `lib/specs.ts` (new, `getProjectSpecs(projectId)`) follow the exact same server-side-fetch-helper pattern `lib/collaborators.ts`'s `getCollaborators` already established — `ProjectSpec` has no stored filename (only `filePath`, the private Blob key per 28's schema), so `filename` is derived as `spec-{id}.md`, matching the exact string the download route already sends via `Content-Disposition` (`trigger/generate-spec.ts` / the download route), so list and download can never disagree on the name.
- 29-spec-ui-integration, the missing-list-endpoint decision: this spec's literal "fetch specs from the backend using the existing ProjectSpec API" assumed a list endpoint that doesn't exist — only the single-spec download route was ever built (28's scope was explicitly download-only). Rather than add a new mutating-adjacent REST route (which this unit's own "do not implement backend logic" scope limit argues against), specs are fetched the same way `app/editor/[roomId]/page.tsx` already fetches `ownedProjects`/`sharedProjects`/`collaborators` — a server-side Prisma read via a `lib/*.ts` helper, called directly in the page and threaded down as a prop (`page.tsx` → `WorkspaceShell` → `AiSidebar` → `SpecsTab`), not a client-side `fetch` to a new API route. This satisfies "do not add new global state" more directly than a REST route would have, and matches this project's established precedent for read-only per-room data. The preview modal's content fetch is the one place that *does* call an HTTP endpoint from the client — the pre-existing download route, read via `fetch().then(r => r.text())` rather than navigated to, which is what "fetch spec content through an existing endpoint" could actually mean literally (it already existed).
- 29-spec-ui-integration: installed `react-markdown` (`^10.1.0`, no new transitive HTML-sanitization risk — it does not render raw HTML by default, only actual Markdown AST nodes) to satisfy "render content as Markdown." Custom `components` mapping (in `components/editor/ai-sidebar.tsx`) styles every element with the app's existing token utilities (`text-copy-primary`, `bg-subtle`, `text-brand`, etc.) instead of the Tailwind Typography plugin's `prose` classes — that plugin isn't installed in this project, and its own color defaults would have conflicted with `code-standards.md`'s token-only color rule.
- 29-spec-ui-integration: `components/editor/ai-sidebar.tsx`'s `SpecsTab` is now real — a `ScrollArea`-scrolled list of spec rows (filename + formatted `createdAt`, a `FileText` icon, a per-row `Download` icon button), empty state when `specs` is empty, and a `SpecPreviewDialog` (shadcn `Dialog`) opened by clicking a row. The dialog fetches content itself (`useEffect`, keyed on `spec`/`projectId`) and renders it via `ReactMarkdown`; content is held in a small local status union (`idle | loading | error | success`, not separate booleans — the first draft's separate `isLoading`/`content` `useState` calls plus a synchronous `setState` at the top of the effect body tripped the newer `react-hooks/set-state-in-effect` lint rule, fixed by moving the fetch into a nested `async function load()` invoked from the effect instead) and is reset via the effect's own cleanup the moment the spec changes or the dialog closes — never persisted anywhere longer-term, per this unit's explicit "do not store spec content in frontend state long-term." The dialog's own footer `Download` button and each list row's icon button both call the same `downloadSpec()` helper (creates a temporary `<a download href="/api/projects/{projectId}/specs/{specId}/download">`, clicks it, removes it) — the route's own `Content-Disposition: attachment` header is what actually makes the browser download rather than navigate, so this is "let the browser handle it," not custom download logic. The pre-existing "Generate Spec" button was left in place, unwired — out of this unit's scope (see Current Goal).
- 29-spec-ui-integration: verified via `tsc --noEmit` (clean), `eslint` (clean after the set-state-in-effect fix above), `npm run build` (passes, no new routes — this unit added no API routes), and a live end-to-end Playwright pass against the project's already-running `next dev`/`trigger dev`, using the established `@clerk/backend` throwaway-user + Bearer-token pattern (sufficient here since, unlike the chat tabs, `SpecsTab`/`SpecPreviewDialog` need no `useUser()` — the Bearer header rides along on the page's own client-side `fetch` calls to the same-origin download route, same as any other request from that browser context). Seeded a real `Project` + a real `ProjectSpec` (actual Markdown uploaded to Vercel Blob, mirroring `generate-spec.ts`'s own write path exactly) rather than mocking anything. Confirmed: the real spec's filename and date render in the list; clicking it opens the modal and renders real Markdown structure (an `<h1>Overview`, a list item "Gateway" — not just raw text); the modal's Download button and the list row's own Download button both produced a real browser download event with the correct `spec-{id}.md` filename; Escape closed the modal. One environment note worth keeping for future sessions: `page.goto(..., {waitUntil:"networkidle"})` never resolves on any `/editor/[roomId]` route in this app — the Liveblocks WebSocket connection keeps the network from ever going idle — use `"load"` instead (this session's own first attempt hit exactly this 30s timeout before switching). All seeded data (project, spec, its blob) and the throwaway Clerk user were removed afterward.

## Next Up

- Resolved by 29-spec-ui-integration: the "Specs" tab is no longer 20-ai-sidebar-shell's static demo card — it now lists real `ProjectSpec` rows for the current room, previews their real Markdown content in a modal, and both the list and modal can trigger a real file download. Still not resolved: the "Generate Spec" button itself is still inert — no unit has yet wired the frontend to actually call `POST /api/ai/spec` and trigger a real `generate-spec` run; a future unit would need to add that (mint/consume `POST /api/ai/spec/token` the same way 26-design-agent-frontend's `useRealtimeRun` pattern already does for `design-agent`, then presumably `router.refresh()` or similar so the now-real spec shows up in this unit's list without a manual reload).
- Resolved by 28-spec-persistence-download, and its frontend gap resolved by 29-spec-ui-integration: the generated spec is no longer unpersisted — `generate-spec` writes its Markdown to Vercel Blob and a `ProjectSpec` metadata row immediately after generation, `GET /api/projects/[projectId]/specs/[specId]/download` retrieves it, and the Specs tab now lists/previews/downloads real specs through that same route (see 29's Completed notes). Still not resolved: triggering `generate-spec` itself from the UI — see 29's Next Up bullet above.
- Resolved by 27-spec-generation-flow: `trigger/generate-spec.ts` is no longer a stub — see Completed above. Its frontend display/download gap was resolved by 29-spec-ui-integration; triggering a new run from the UI is still open (see above).
- Resolved by 26-design-agent-frontend: 23-design-agent-logic's "chat is still not persisted or Liveblocks-synced as *conversation history*" is now resolved for the Architect tab specifically — its prompts and Ghost AI's replies are both persisted in the `ai-chat` feed and survive a reload/new session. `POST /api/ai/design/token` is now called implicitly (the same mint logic runs inside `/api/ai/design` itself) but the standalone `/token` route remains unused by the frontend.
- Resolved by 25-sidebar-chat-feed: the AI sidebar now has a third "Chat" tab backed by a real, room-scoped, persisted Liveblocks Feed (`ai-chat`) — collaborators can send and receive plain-text chat messages in realtime, validated on read, kept fully separate from `ai-status-feed`. No read receipts, typing indicators, message editing/deletion, or pagination/"load more" UI (feed messages beyond the default page size aren't fetched) — none of these were asked for by this unit's spec. No moderation/rate-limiting on send either.
- Resolved by 24-ai-presence-state: the AI sidebar now shows a small, shared (room-wide) "AI is working" status indicator driven by the `ai-status-feed` `RoomEvent`, with the chat input and send button disabled/loading while generation is active, and live cursors show a spinner for any participant (not just the AI) with `presence.thinking: true`. Still no persisted/synced chat history (unchanged from 23 — see below) and no per-run/per-user scoping of the shared "is generating" state: if two people are in the same room, one person's in-flight prompt disables the input for everyone, and a participant who opens the room mid-run sees no indicator until the next event fires (RoomEvents are not replayed to late joiners, same limitation the chat transcript already had since 23). A future unit could address either with the AI's own presence entry (`AI_AGENT_USER_ID`, already synced on join, unlike events) as a more join-safe source of truth for "is AI currently working," if this becomes a real problem.
- The `LIVEBLOCKS_SECRET_KEY` gap flagged by 10-liveblocks-setup and 11-base-canvas is resolved — a real key is now in `.env.local` and this unit's live verification confirmed the canvas actually connects and syncs for real. Still outstanding from that old note: `RoomConnectionGuard`'s `useErrorListener` fallback (`components/editor/canvas-room.tsx`) has still never been observed actually firing `CanvasConnectionError` for a genuine access-denied/room-full case (only the old indefinite-retry case was ever observed, back when the key was a placeholder) — a future unit touching connection-error handling should verify this directly (e.g. a room a user has no access to).
- Resolved by 15-node-color-toolbar: selected nodes now show a floating swatch toolbar (`components/editor/node-color-toolbar.tsx`) that can set any node's color to any `NODE_COLORS` pair, both at creation-default time and after. Dropped nodes still start at `DEFAULT_NODE_COLOR` (unchanged, still correct per 12-shape-panel) but are no longer stuck there.
- No node **shape** editing after creation (can't change a node's shape post-drop) and no properties panel. Not in this unit's spec.
- Resolved by 13-node-shape: `CanvasNodeRenderer` now renders shape-specific visuals (CSS for rectangle/pill/circle, inline SVG scaled to node size for diamond/hexagon/cylinder), per `ui-context.md`'s "rendered as inline SVGs rather than CSS borders" note. No migration was needed for nodes created under the old generic renderer — `shape` was already stored correctly in `data`, only the renderer changed.
- Resolved by 14-node-editing: nodes can now be resized (with a `NODE_MIN_SIZE` floor) via drag handles shown on selection, and labels can be set/edited via double-click-to-edit with a live-synced textarea. Both flow through the existing Liveblocks-backed `onNodesChange` state, verified to survive a page reload.
- Resolved by 16-edge-behavior: `CanvasNodeRenderer` now renders a connection `Handle` on all four sides (hidden by default, faded in on node hover, per `ui-context.md`), and `Canvas`'s new `defaultEdgeOptions` routes every connection through a custom `canvasEdge` renderer (right-angle routing, arrowhead, dim-at-rest/bright-on-hover-or-select, inline label editing via `EdgeLabelRenderer`). Nodes can now be connected and labeled via the UI, verified end-to-end including a page reload.
- Resolved by 17-canvas-ergonomics: `<Controls />` (the built-in xyflow component) is still not used, but its zoom/fit functionality is now covered by the custom `CanvasControlBar` (bottom-left pill, zoom in/out/fit view + undo/redo) plus `hooks/use-keyboard-shortcuts.ts` (`+`/`-`/`Cmd·Ctrl+Z`/`Cmd·Ctrl+Shift+Z`/`Cmd·Ctrl+Y`). The `MiniMap` added in 11-base-canvas has been removed per this unit's explicit spec.
- Resolved by 19-presence-avatars-cursor: `components/editor/presence-avatars.tsx` now shows a collaborator avatar stack + the current user's `UserButton` in the canvas's top-right corner, and `components/editor/presence-cursors.tsx` renders other participants' live cursors (broadcast via `<ReactFlow onPaneMouseMove>`/`onPaneMouseLeave`). No node-level "who's viewing/editing this node" indicator exists yet — this unit was canvas-wide presence only, not per-node.
- Resolved by 20-ai-sidebar-shell: the AI sidebar now has a real UI shell (header, `AI Architect`/`Specs` tabs, chat empty state + starter chips + input, demo spec card) as a floating slide-in panel (`components/editor/ai-sidebar.tsx`). Still no real AI chat, no backend/Liveblocks-synced messages or specs, and no spec generation — chat messages are local-only UI state, per this unit's explicit scope limits. `ProjectStatus` on `Project` is still unused by any UI.
- Resolved by 21-canvas-autosave: the canvas now autosaves to Vercel Blob (debounced, `hooks/use-canvas-autosave.ts`) with `Project.canvasJsonPath` as the Prisma-side pointer, loads a saved canvas into a genuinely empty room on mount, and shows live save status in the workspace navbar. Every collaborative client independently watches `nodes`/`edges` and debounce-saves on its own — with several people editing at once, every connected client will fire its own autosave request on every change, not just one "leader" client. This is harmless (idempotent same-project overwrites, `allowOverwrite: true`) but is real write amplification in a busy room; not addressed here since the spec only asked for per-client debouncing, not cross-client save coordination. A future unit could elect a single saver (e.g. lowest connection ID) if this becomes a real cost/rate-limit concern.
- The create dialog's "Room ID" preview (`slugify(name)-{shortSuffix}`, generated in `hooks/use-project-actions.ts`) is cosmetic only — confirmed again this unit: the real Liveblocks room ID is `project.id`, exactly as `app/api/liveblocks-auth/route.ts` uses it. Don't wire any future `RoomProvider` to the preview string.
- 09-share-dialog invites by email only — there is still no local user table (per that unit's explicit "do not add a local user table" rule), so an invited email that has never signed up shows as email-only (no Clerk match) until/unless they create a Clerk account with that exact address. No invite notification (email, in-app, etc.) is sent; the spec didn't ask for one.
- 18-starter-template shipped exactly the scope its spec allowed: no template saving, no custom/user-authored templates, no server persistence for templates (matches `architecture-context.md`'s pre-existing "Starter System Designs" section, which already documented templates as static codebase snapshots resolved by ID at import time — no changes were needed there). A future unit adding "save current canvas as a template" would be new scope, not an extension of this one.
- Resolved by 23-design-agent-logic: `components/editor/ai-sidebar.tsx`'s "AI Architect" tab now calls `POST /api/ai/design` for real, and `trigger/design-agent.ts` has real Gemini-backed generation logic that writes into the room's shared Liveblocks Storage, with AI presence and a shared status feed. `POST /api/ai/design/token` and `useRealtimeRun` went unused by the frontend until 26-design-agent-frontend wired both in (see that unit's Completed notes).
- Resolved by 26-design-agent-frontend: chat is no longer local-component-state-only — the Architect tab's prompts and Ghost AI's replies now persist in the `ai-chat` feed and survive a reload or a second session (see that unit's Completed notes, including the deliberate decision to merge this content with the human Chat tab's feed rather than add a second one).
- 23-design-agent-logic: no cross-run/cross-user coordination — nothing stops two users (or one user twice) from triggering overlapping `design-agent` runs in the same room at once. Each run reads the canvas independently at its own start, so two concurrent prompts could both offset their new content against the *same* pre-run bounding box and land on top of each other. Not addressed here (the spec didn't ask for it); a future unit could serialize runs per room (e.g. a Trigger.dev `concurrencyKey` on the room id) if this becomes a real problem.
- 23-design-agent-logic: `trigger/generate-design.ts` and `trigger/generate-spec.ts` were, at the time of this unit, still untouched, unimplemented stubs (see 22-design-agent-api's note and the Open Question below) — `design-agent.ts` was the only task with real logic. Resolved by 27-spec-generation-flow: `generate-spec.ts` now has real logic; `generate-design.ts` is still an unused stub. Spec generation's *frontend* (the "Specs" tab's `Generate Spec` button) is still fully out of scope — still a static demo card from 20-ai-sidebar-shell; 27-spec-generation-flow was backend-only by explicit scope limit.

## Open Questions

- Resolved by 23-design-agent-logic: `design-agent.ts` uses `@ai-sdk/google` (`generateObject`) against `google("gemini-flash-latest")`, authenticated via `GOOGLE_AI_API_KEY` (the real env var name present in `.env.local` — `23-design-agent-logic.md`'s spec said `GEMINI_API_KEY`, which does not exist in this project's env files; used what's actually there). `generate-design.ts`/`generate-spec.ts` remain unimplemented stubs — this decision covers `design-agent.ts` only, per the next bullet.
- Still open: 22-design-agent-api introduced `trigger/design-agent.ts` as a third task file alongside the pre-existing `trigger/generate-design.ts`/`trigger/generate-spec.ts` stubs, per that spec's literal file name and payload shape (`{prompt, roomId}`, no `canvas`). 23-design-agent-logic gave `design-agent.ts` real logic; 27-spec-generation-flow gave `generate-spec.ts` real logic. `generate-design.ts` alone remains an unimplemented stub, now overlapping only with `design-agent.ts`'s purpose (spec generation is no longer part of this ambiguity). A future unit should resolve this explicitly — either retire `generate-design.ts` in favor of `design-agent.ts`, or clarify why both are meant to exist. Don't resolve this silently as a side effect of other work — it's its own decision per `ai-workflow-rules.md`.
- Resolved by 28-spec-persistence-download: `architecture-context.md`'s "Spec Generation" section previously said the Markdown spec is "saved to the filesystem," conflicting with `code-standards.md`'s "Canvas snapshots and generated specs belong in Vercel Blob; Prisma stores only the blob URL reference" and with that same file's own Storage Model section (which already specified Vercel Blob at `specs/{projectId}/{specId}.md`). Corrected the stale line to match; `generate-spec` now actually implements the Vercel-Blob-plus-Prisma-pointer pattern (a new `ProjectSpec` model), same as `21-canvas-autosave` established for canvas snapshots.
- `GET /api/projects` still lists only projects where the caller is `ownerId` (per `06-project-apis.md`'s literal rules) — this was intentionally left unchanged by 07-wire-editor-home (see Architecture Decisions below, which resolves the "shared list" question via a new server-only helper instead of modifying this route). If any future client-side code needs to list projects over HTTP (rather than via server-side Prisma calls), it will need either a query param or a separate endpoint for the collaborator case — `GET /api/projects` alone is still owner-only.

## Architecture Decisions

- 26-design-agent-frontend: the AI Architect tab's conversation (design prompts + Ghost AI's replies) is persisted in the exact same room-scoped `ai-chat` Liveblocks feed as the human-to-human "Chat" tab (25-sidebar-chat-feed), not a second feed. This was a real conflict, not an implementation detail: 25 explicitly named `ai-chat` for human chat only and deliberately kept it "fully separate from AI" (that unit's own words), while 26-design-agent-frontend.md's literal text — written independently, apparently without awareness of that prior naming — says to push both the user's prompt and the AI's reply "to the ai-chat feed." A Liveblocks feed message has no "channel"/context field, so satisfying the literal text meant the two tabs would necessarily show each other's messages (a design prompt appears in the Chat tab, a casual chat message can appear in the Architect tab). Per `ai-workflow-rules.md`'s "don't resolve ambiguous requirements as a side effect of other work," this was surfaced to the user directly rather than decided silently; **the user chose to reuse `ai-chat` literally** over introducing a second, separate feed. `types/tasks.ts`'s `chatMessageSchema`/`CHAT_MESSAGE_ROLES` (widened to include `"assistant"`) is the one schema both tabs read/write through. If a future unit needs the two tabs' content kept apart after all, that would mean either adding a channel-discriminating field to the schema or splitting onto two feeds — a new, explicit decision, not a silent revert of this one.
- shadcn/ui initialized with Base UI (not Radix) as the underlying primitive library — this is the CLI's current default, not an explicit choice in the spec.
- Dark theme is forced globally via a `dark` class on `<html>` in `app/layout.tsx`, since `globals.css` only defines a `.dark` override (activated by that class) and a default light `:root` — there was no toggle or existing dark theme to match, so this was the only way to satisfy "no default light styling appears."
- `proxy.ts` uses `@clerk/nextjs/server`'s `createRouteMatcher`, which this Clerk version (`@clerk/nextjs@7.7.1`) marks `@deprecated` in favor of resource-based auth checks (calling `auth.protect()` per page/route instead of centralized path matching). `03-auth.md` explicitly specifies the centralized "define public routes, protect everything else by default" pattern, which is only expressible with a route matcher, so this was implemented as specified rather than substituting the newer per-resource pattern — that would be a bigger architectural change than this unit's scope, and `createRouteMatcher` is deprecated, not removed. Flagging here since AGENTS.md says to heed deprecation notices: if Clerk removes `createRouteMatcher` in a future major version, `proxy.ts` will need auth checks moved into each page/layout/route instead.
- `lib/prisma.ts` uses Prisma 7's native `accelerateUrl` constructor option for `prisma+postgres://` URLs rather than the `@prisma/extension-accelerate` package + `withAccelerate()` extension pattern shown in some Prisma docs/skills — the extension package isn't in `05-prisma.md`'s "already installed" dependency list, and `accelerateUrl` alone is sufficient to connect through Accelerate in v7 (it's mutually exclusive with `adapter` at the type level: `PrismaClientOptionsWithAccelerate` vs. `PrismaClientOptionsWithAdapter` in `node_modules/@prisma/client/runtime/client.d.ts`). If a future unit needs Accelerate-specific features (response caching, connection metrics), that's when the extension package would actually need adding.
- `proxy.ts` now skips `auth.protect()` entirely for `/api/*` (early-return via an `isApiRoute` matcher) instead of applying the same centralized protect-everything pattern used for pages. Reason: Clerk's `auth.protect()` redirect-vs-401 decision is page-request heuristics that misfired for API routes in this Next/Clerk combination (see the 06-project-apis bugfix note above) — API routes now enforce auth themselves via `auth()` + explicit `401` JSON, which `code-standards.md` already required of route handlers regardless. This narrows, not removes, `03-auth.md`'s centralized-protection pattern: it still applies to every page route.
- `ui-context.md` documents Ghost-specific CSS variable names (e.g. `--bg-base`) and gives a few example Tailwind utility names (`bg-base`, `text-copy-primary`, `text-copy-muted`, `border-surface-border`, `text-brand`, `bg-accent-dim`) but not an exhaustive mapping. Extended that pattern consistently for the remaining tokens (e.g. `text-copy-secondary`, `text-copy-faint`, `border-surface-border-subtle`, `text-ai`/`bg-ai`, `text-ai-text`, `text-error`/`text-success`/`text-warning`). These are additive to shadcn's existing `--primary`/`--background`/etc. tokens (kept as-is for `components/ui/*` internals) — the Ghost tokens are for app-level/feature components.
- 07-wire-editor-home: `app/api/projects/route.ts`'s `GET` (owner-only list, from 06-project-apis) was intentionally *not* refactored to use the new `lib/projects.ts` helper or extended to include collaborator projects. This unit's spec said to fetch owned+shared projects "server-side using the existing project data helper" for the page itself — read as "add the helper and use it in the server component," not "change the already-shipped, already-verified API route's contract." Nothing in the app calls `GET /api/projects` over HTTP anymore (the sidebar's data comes from the layout's direct Prisma calls); if a future unit needs the API route itself to return collaborator projects too, that's a separate, explicit decision — resolving it silently as a side effect here would have violated "do not combine unrelated system boundaries in a single implementation step."
- 07-wire-editor-home: introduced `app/editor/layout.tsx` (new) as a nested layout so both `/editor` and `/editor/[projectId]` share one server-side project fetch and one client-side `EditorShell` (navbar/sidebar/dialogs) instance without duplicating either. This is the first nested layout in the app; chosen over having each page fetch and render its own copy of the chrome because layouts persist across navigations within the same segment (per Next's docs), which is what makes the sidebar/dialogs feel like a stable app shell rather than remounting on every project switch — the tradeoff (documented above under `use-project-actions.ts`) is that layout data is *not* automatically refreshed on sibling navigation, so mutations must call `router.refresh()` explicitly.
- 11-base-canvas: the spec's "error fallback for Liveblocks connection issues" is implemented via `useErrorListener` (`components/editor/canvas-room.tsx`'s `RoomConnectionGuard`), not by relying on the adjacent `ErrorBoundary` to catch it. Liveblocks' room-connection/auth failures do not throw a React render error — `ClientSideSuspense` will happily sit in its loading fallback indefinitely (retrying in the background) rather than ever rejecting into the `ErrorBoundary`. This matches the `liveblocks-best-practices` skill's own `handling-connection-errors` reference, which documents `useErrorListener` + `error.context.type === "ROOM_CONNECTION_ERROR"` as the mechanism for exactly this case, separately from the `rendering-loading-components` reference's `ErrorBoundary`+`ClientSideSuspense` pattern (which is for actual render-time errors in complex data hooks). Both are kept — they cover different failure classes.
- 08-editor-workspace-shell: superseded the previous decision above — `/editor/[roomId]` needed a structurally different top navbar (project name + share + AI toggle) than `/editor` home's (`EditorNavbar`: sidebar toggle + `UserButton` only), and Next layouts can't opt a child segment *out* of a parent layout while keeping siblings in it. Used the documented route-group pattern (`node_modules/next/dist/docs/.../route-groups.md`, "opting specific route segments into sharing a layout, while keeping others out") to split `app/editor/` into `app/editor/(home)/` (keeps the old `EditorShell` layout, URL still `/editor`) and a sibling `app/editor/[roomId]/` with its own self-contained `WorkspaceShell` (fetches its own project lists, owns its own `useProjectActions()`). Cost: `getOwnedProjects`/`getSharedProjects` are now called from two places (`(home)/layout.tsx` and `[roomId]/page.tsx`) instead of one shared layout — accepted because the alternative (threading room-specific navbar state up through `EditorShell` via context, the pattern already used for `openCreateDialog`) would have made the generic home chrome conditionally room-aware, which is a worse coupling than one duplicated data-fetch call. `app/editor/` itself has no `layout.tsx` of its own now — root layout (`app/layout.tsx`) is unaffected, so this isn't the "multiple root layouts" case the route-groups doc warns about (no full-reload-on-navigation cost).
- 21-canvas-autosave: this app's Vercel Blob store is configured **private**, not public — confirmed by a real runtime error (`Cannot use public access on a private store`) when `app/api/projects/[projectId]/canvas/route.ts`'s first pass used `access: "public"`. Every future read/write against this store (canvas JSON here, and per `code-standards.md`'s "Canvas snapshots and generated specs belong in Vercel Blob," presumably generated specs too, once that unit exists) must use `access: "private"` on both `put()` and `get()` — a plain unauthenticated `fetch()` against a private blob's URL 403s, so reads must go through `@vercel/blob`'s `get()` helper (authenticates via `BLOB_READ_WRITE_TOKEN`, same as `put()`), not a raw `fetch`.
- 18-starter-template: **`@liveblocks/react-flow`'s `onNodesChange`/`onEdgesChange` silently no-op on a `"remove"` change** — confirmed by reading `node_modules/@liveblocks/react-flow/dist/lib/flow.js`'s `applyNodeChanges`/`applyEdgeChanges` (`case "remove": break;`, for both). The only mutation that actually deletes from the room's `LiveMap`s is `onDelete({nodes, edges})` (already wired to `<ReactFlow onDelete={onDelete}>` since 11-base-canvas, for React Flow's built-in delete-selection handling). Any future unit that needs to programmatically remove nodes/edges — not just add or update them — must call `onDelete`, not construct a `"remove"` `NodeChange`/`EdgeChange`. This surfaced as a real bug during this unit's own live verification (a manually-dropped node survived a template import that was supposed to clear the canvas first) before the fix.
- 23-design-agent-logic: a background Trigger.dev task can mutate a room's Liveblocks Storage directly and safely, bypassing React entirely, via `@liveblocks/node`'s `Liveblocks.mutateStorage(roomId, ({root}) => ...)` + its own exported `LiveObject`/`LiveMap` classes (confirmed live, not just from docs) — as long as the room's `Storage` type (in `liveblocks.config.ts`) actually describes the real tree shape, since `mutateStorage`'s `root` param is typed against that same global declaration. This is the general mechanism for any future "AI/backend edits the shared canvas" work, not specific to node/edge writes — the same approach would apply to any other Storage-backed feature a background task needs to touch.
- 23-design-agent-logic: for structured AI output that a background task will mechanically apply (as opposed to output a human just reads), prefer **one schema array per distinct operation type** over a single polymorphic/discriminated array. This project's real Gemini key rejected a `zod.discriminatedUnion` outright, and a flatter "one shape, most fields optional" schema validated but produced unreliable output (missing required fields, duplicate entries) because the model had no schema-level signal for per-type requiredness. Homogeneous, mostly-required-field arrays fixed both problems. Worth defaulting to this shape for any future structured-generation task in this project (e.g. `generate-spec.ts`, if it ever needs more than plain Markdown).
- 23-design-agent-logic: don't trust a spec's stated env var name over what's actually in `.env.local` — `23-design-agent-logic.md` said `GEMINI_API_KEY` was already present; the real key is named `GOOGLE_AI_API_KEY`. Always grep the actual env files before wiring a provider, even when the spec is explicit about the name.

## Session Notes

- No `chromium-cli` or local `playwright` install was available in this environment; used `npx playwright@1.62.1 install chromium` + a local scratchpad install to drive a temporary route (`app/editor-preview-tmp`, deleted after use) for visual verification. If this recurs often, consider `/run-skill-generator` to capture it as a project skill.
- 07-wire-editor-home verification needed a real authenticated *browser* session (not just raw `fetch` like 06), since the thing under test was server-rendered pages + client dialogs, not just API responses. Used `npx tsx` to run a TypeScript script directly (so it could import `@/lib/prisma` and other path-aliased app code without a build step), driving Playwright with `page.context().setExtraHTTPHeaders({ Authorization: "Bearer <clerk session token>" })` — Clerk's `auth()`/`authenticateRequest` accepts a Bearer token the same way for full page navigations as it does for API routes, so this authenticates SSR `app/editor` page loads without needing to fake browser cookies. Playwright itself was symlinked into the project's `node_modules` from a scratchpad install (no local install available, same constraint noted in 02-editor) and removed afterward; the driver script lived at a temporary `scripts/tmp-e2e-07.ts`, deleted after the run.
- Running `next dev` auto-regenerates part of `AGENTS.md` (a Next.js 16 built-in "agent rules" feature — see the `<!-- END:nextjs-agent-rules -->` block). This happened as a side effect of dev-server verification in this session; it's expected framework behavior (per the injected note, `next dev` re-adds it every time), not a manual edit. The regenerated block is included in the `AGENTS.md` change committed on the `development` branch — set `agentRules: false` in `next.config.ts` if you'd rather it not regenerate on future `next dev` runs.
- 11-base-canvas: the live Playwright check for this unit reused the `page.context().setExtraHTTPHeaders({ Authorization: "Bearer <clerk session token>" })` pattern from 07/08. That header is sent on *every* request from that browser context, including the third-party requests Clerk's client JS makes to load its own SDK from `*.clerk.accounts.dev` — in this run that produced a CORS/redirect failure on those specific requests and a `failed_to_load_clerk_js` console error. Confirmed this is a test-harness artifact, not an app or environment defect: re-ran the same check against `/sign-in` (no `Authorization` header involved in that page's normal flow) with zero page errors. Not investigated further since it doesn't affect real users (who never send a global `Authorization` header to a CDN), but worth remembering for any future session reusing this Bearer-token Playwright pattern on a page that also needs Clerk's client-side JS to load.
- Reported in `current-issues.md`: an `unhandledRejection` in the browser console on `/editor/[roomId]` — `ClerkJS: Network error at ".../v1/client/sessions/.../touch?..." - TypeError: Failed to fetch`. This is Clerk's client-side SDK pinging its own hosted "keep session active" endpoint (`*.clerk.accounts.dev`), entirely external to the app — it doesn't pass through `proxy.ts`, any `/api` route, or Prisma, and isn't on any path Features 08/09 touched. It fired 6 seconds after a `next dev` restart, consistent with a page load racing a server restart/reload (this session did many restarts + automated Playwright reloads while verifying 08/09) — Clerk's SDK doesn't catch that particular cancellation gracefully, so it surfaces as an unhandled rejection instead of failing silently. Confirmed transient: reloading the same URL once made it disappear, with no code changes. Treat this the same as the existing "development keys" and `createRouteMatcher` deprecation console notices — expected dev-mode noise, not an app defect. No action taken.
- 12-shape-panel: reused a prior scratchpad Playwright/Chromium install (symlinked into `node_modules/playwright`, removed after use — same pattern as 07) since none is installed in this project. Native OS-level HTML5 drag-and-drop can't be reliably driven through Playwright/CDP mouse simulation in a headless context, so the verification instead called `page.evaluate()` to construct a real `DataTransfer` and dispatch actual `dragstart`/`dragover`/`drop` `DragEvent`s at the toolbar button and canvas pane elements — this exercises the app's real event handlers end-to-end (unlike, say, calling a React prop function directly), just without a real OS drag gesture. Worth reusing this technique for any future drag-and-drop verification in this app (e.g. reordering, resizing).
- Also discovered this session: `LIVEBLOCKS_SECRET_KEY` in `.env.local` is now a real key, not the empty placeholder 10-liveblocks-setup and 11-base-canvas both left as an open gap — someone (not this session) must have pasted one in since 11-base-canvas ran. This unintentionally resolved part of both units' outstanding "re-verify once a real key exists" follow-ups; see the 12-shape-panel Completed note above for what was (and wasn't) re-confirmed as a result.
- 18-starter-template: reused a prior scratchpad Playwright/Chromium install again (symlinked into `node_modules/playwright`, removed after use) plus the established `@clerk/backend`-created-user + Bearer-token pattern. Two new environment notes for future sessions: (1) Clerk's `createUser` rejects `*.test`-TLD email addresses with `form_param_format_invalid` — use `*.example.com` instead; (2) Clerk's password policy on this instance requires 15+ characters, shorter generated passwords 422 with `form_password_length_too_short`. Also: `npx tsx` does not auto-load `.env.local`/`.env` the way `next dev` does — verification scripts need `-r dotenv/config` (with `DOTENV_CONFIG_PATH=.env.local`) or they'll fail on missing `CLERK_SECRET_KEY`/`DATABASE_URL` even though those keys are genuinely present in `.env.local`.
- 21-canvas-autosave: reused a prior session's scratchpad Playwright/Chromium install again (symlinked into `node_modules/playwright`, removed after use) — same recurring gap noted since `02-editor`: no local Playwright/Chromium install exists in this project itself. Also hit a `page.waitForFunction(fn, {timeout})` footgun worth remembering for future scripts: `waitForFunction`'s signature is `(pageFunction, arg?, options?)` — passing an options object as the second positional argument (omitting `arg`) is silently treated as `arg` (passed into the browser-side function), not as `options`, so the real timeout falls back to Playwright's 30s default. Must pass `undefined` explicitly for `arg` when only `options` is needed: `waitForFunction(fn, undefined, {timeout})`.
- 19-presence-avatars-cursor: tried to work around the long-documented Bearer-token-vs-Clerk-CDN CORS artifact by scoping the synthetic `Authorization` header to same-origin requests only (`context.route("**/*", ...)`, conditionally adding the header only when `new URL(request.url()).hostname === "localhost"`), instead of `context.setExtraHTTPHeaders` (which applies the header to every request from that context, third-party CDN calls included). This did stop the CORS console errors, but `UserButton` still never mounted (`data-clerk-component="UserButton"` stayed empty even after a 20s explicit `waitForSelector`) — worse than the plain `setExtraHTTPHeaders` approach, which at least intermittently got far enough to show a fallback avatar glyph in a screenshot. Root cause not isolated (possibly route-interception overhead/latency on every single request, including Next's dev HMR/chunk requests) — reverted to `setExtraHTTPHeaders` for this unit's verification. Worth real investigation in a future session if a unit ever needs to confirm `UserButton`'s actual rendered *contents* (not just that it mounts) under the Bearer-token pattern — until then, treat any Clerk-JS-dependent widget as unverifiable-by-screenshot under this harness, confirm everything else via DOM structure/network assertions instead.
- 22-design-agent-api: `npx trigger.dev@4.5.10 dev`'s CLI output is easy to misread as hung — after "Local worker ready on branch: default [node] -> ..." it prints nothing further and just sits there for a while with no visible progress. This is not stuck: that line plus the "Key: Version | Task | Run" header above it *is* the fully-connected steady state; the CLI is a live table that only appends a row (via `\r`-based redraws — pipe the log through `tr '\r' '\n'` if capturing to a file for grep/tail, since raw `\r`s look like one giant line otherwise) when a run actually happens, first a "running" row then a "Success (Nms)"/failure row for the same run ID. Don't kill and restart it after ~30-60s of apparent silence; trigger a real run against it (or just wait) to confirm it's actually connected. Also reconfirmed the `npx tsx -r dotenv/config` + `DOTENV_CONFIG_PATH=.env.local` pattern from 18-starter-template's note, and reused the same `@clerk/backend` throwaway-user pattern as every prior live-verification session.
- 23-design-agent-logic: `trigger dev`'s file-watcher rebuilds the worker automatically on save (`○ Rebuilding local worker… / ○ Local worker ready`) — no restart needed between schema/logic iterations, only a few seconds' wait for the new version to register before triggering another run. Used the `mcp__trigger__get_run_details`/`mcp__trigger__get_span_details` MCP tools (not just tailing the CLI log) to read a run's actual output and `logger.log` attributes — the CLI table only shows a pass/fail summary line per run, not log payloads; `get_span_details` on a specific logged span's id (from `get_run_details`'s trace tree) is what actually surfaced the model's malformed output during debugging (see the schema-design note above), which the plain CLI log never showed. `get_run_details`/`get_span_details` need an explicit `projectRef` in this environment (auto-detection failed) — read it from `.env.local`'s `TRIGGER_PROJECT_REF`.
- 23-design-agent-logic: confirmed live against this project's actual Google AI key (not assumed from docs) that `models.list` returning a model id doesn't mean `generateContent` will actually accept it — `gemini-2.5-flash` is listed but rejected live with "no longer available to new users." When in doubt about a Gemini model id, a raw `curl .../v1beta/models/<id>:generateContent?key=...` smoke test is faster and more trustworthy than either assuming a docs-remembered id or trusting `models.list` alone.
- 23-design-agent-logic: reused a prior session's scratchpad Playwright/Chromium install again (symlinked into `node_modules/playwright`, removed after use — same recurring gap noted since `02-editor`) for the browser verification pass. Hit the same class of click-interception issue 17-canvas-ergonomics documented for the Next.js dev-tools badge, this time from Liveblocks' own bottom-right branding badge (`#liveblocks-badge`) intercepting a real `.click()` on a button underneath it — worked around by dispatching the click via `locator.evaluate(el => el.click())` (bypasses Playwright's actionability/hit-testing entirely) rather than `{force: true}` (which still enforces viewport-visibility and failed here). Also: Liveblocks Storage updates from a single atomic server-side `mutateStorage()` call can still arrive at a connected client as multiple incremental renders (node count observed going 0 → 1 → 4, not straight to 4) — any future test asserting a final node/edge count after an AI/backend write should poll until the count stabilizes, not just wait for it to exceed zero.
- 25-sidebar-chat-feed: the established `Authorization: Bearer <session token>` Playwright pattern (used by nearly every prior unit's live verification, per the many "Bearer-token-vs-Clerk-CDN CORS artifact" notes elsewhere in this file) does not work for any feature that depends on `useUser()`/`useOthers()`/Clerk's client-side SDK actually resolving real user data — this unit's Chat tab reads `user.fullName` from `@clerk/nextjs`'s `useUser()` to stamp each outgoing message's `sender`, and under the Bearer-header pattern `useUser()` stays permanently null (clerk-js itself fails to load, same CORS failure previously observed, so `user` never resolves), which silently no-ops every send with no visible error. Switched this unit's verification to **real cookie-based sign-in** instead: drive Clerk's actual `/sign-in` form (`#identifier-field`/`#password-field`, single-step in this instance's config — both fields present on one screen, no separate identifier-then-password steps) via Playwright, which loads clerk-js normally (no interfering global header) and yields a real session cookie. New-device/new-IP sign-ins on this Clerk instance trigger a device-verification ("client-trust") interstitial ("Check your email") — worked around using Clerk's documented test-email convention: an email containing `+clerk_test` (e.g. `name+clerk_test@example.com`) makes Clerk accept the fixed dev OTP `424242` instead of sending a real email. This is a materially different (and more broadly useful) technique than the Bearer-token pattern for any *future* unit whose verification needs real client-side Clerk user data (not just an authenticated page load) — worth reaching for first over the Bearer-token pattern when that's the case.
- 25-sidebar-chat-feed: confirmed live (`node_modules/@liveblocks/core/dist/index.js`'s `FEEDS_TIMEOUT = 5e3`) that feed mutations (`createFeedMessage` etc.) time out client-side after 5s if the server never responds (e.g. genuinely offline, via Playwright's `context.setOffline(true)`) — safe to test the "send failed" UI path with a real offline toggle instead of needing to mock the hook, since the promise is guaranteed to settle within 5s rather than hanging forever.
